# CLAUDE.md

@AGENTS.md

## 决策模式：做出来给用户看，不要在中途问

产品负责人 2026-08-15 明确规定：

> 「我只能在看到最终结果有问题之后才能给出决策。中间多次询问我这种开发模式
> 应该变更。」
> 「我只负责出 UI/UX 的产品要求。」

因此本项目的决策输入点是**用户看到可运行结果之后**，不是开发中途。

**唯一的判据不是「这个决定重不重要」，而是「错了能不能重来」。**
能做出来给用户看、看完不满意再改的 —— 直接做。做了就回不了头的 —— 先问。

### 直接自己定，不问（绝大多数情况）

- **一切技术决策**：架构与模块边界、持久化 schema 的形状与迁移策略、数据结构、
  接口与跨层合同、依赖选型、错误处理与并发模型、测试与审查策略、工具与配置、
  命名与文件组织。定完按 AGENTS.md 第 18 条写进 `docs/`，**不上交**。
- **UI/UX 也先做一版给用户看**，而不是问「你要 A 还是 B」——用户在看到之前
  无法判断，问了等于把工作退回给他。做出来、能演示、他看完给反馈、再改。

### 「回不了头」是缺陷，先消除它，而不是拿它去问用户

产品负责人 2026-08-15：

> 「做了就回不了头的才问这一点也很奇怪。按理说不应该回不了头啊。这时候就算
> 问我我也不知道该如何回答。只能选择推荐选项。」

这是对的：不可逆是**实现方式的缺陷**，不是一个该上交的选择题。遇到它，默认
动作是**把它变成可逆的**：

- 覆盖用户文件 → 写**带版本的新路径**（AGENTS.md 第 13 条本来就允许这条路）
- 数据迁移 → 迁移前留下可回滚的旧数据；**加法字段优先于破坏性变更**
- 删除 → 软删除 / 移入回收区，保留历史版本
- 状态机 → 留一条回退边，而不是单向门

只有在**确实无法消除不可逆性**时才问，并且必须带明确推荐。

### 真正必须问的只有一件：付费 / 真实花钱

花的是用户的钱，不是技术选择；用户在这件事上有 Agent 没有的信息（预算、
这次值不值得）。ADR-0006 / ADR-0009 的窄授权不得自行扩大。

除此之外，「需要产品负责人拍板」不是一个有效的理由。
**在技术问题上停下来问，等于把工作退回给用户。**

### 不得不问的时候，问法也有要求

用户明确说过「就算问我我也不知道该如何回答，只能选择推荐选项」——所以开放式
选择题等于把判断退回给他，一次纯浪费的往返。

**正确形式：说明你打算做什么、为什么、风险是什么，让用户否决。**
不是「A 还是 B？」，而是「我打算 A，因为 X，代价是 Y——要拦吗？」
前者需要他做判断，后者只需要他点头或拦下。

不得把决策包装成「有三个方案，请你选择」。开工前的文档收口只做一轮；
同一批次开工前出现第二、第三轮纯文档收口，说明该决策已被过度前置——
把未定项作为显式假设写进任务卡然后开工，别再收口一轮。

### ADR 的 Accept 权

技术 ADR（架构、schema、迁移、合同、并发、测试与审查节奏、工具链）由实施
Agent 依本节授权**自行 Accept**，并在 ADR 里写明依据与理由。涉及**付费**或
**不可逆动用户数据**的 ADR 仍须用户 Accept。UI/UX 相关的 ADR 先实现出可演示
的版本，再由用户在看到结果后确认或推翻。

## 范围与切片

- 每个需求按**垂直切片**推进：每个切片自己就能跑、能演示、能验证。不要按
  技术层拆（schema → resolver → service → store → component → UI），那会让
  用户直到最后才看到东西。
- 任务开始时写明 **IN SCOPE / OUT OF SCOPE**。实施中发现的非阻塞问题记为
  `Follow-up`，不顺手修。
- 非本任务 bug：一律**记录**到 `Follow-up` 或新任务卡，**不顺手修**
  （AGENTS.md 第 17 条）。唯一例外是它**阻塞当前任务**——那时只在最小范围内修，
  并在报告里写明为什么绕不开。P2 记录；**P3/P4 不修**。
  不要因为审查者报了 5 个小问题，把一个用户功能扩成两天重构。
- 技术基础只做 **Minimum Necessary Foundation**。基础工作开始膨胀时先问：
  「不做这个重构，当前用户功能能否安全完成？」能，就不做。
- 同一时间最多推进 1 个主要用户需求 + 1 个阻塞它的技术任务。
  **Finish before starting more.**

## Claude Code 专用规则

- **技术架构变更不需要用户确认**——它属于技术决策：自己定、实施、写进 `docs/`。
  只有付费与不可逆动用户数据这两项才先停下来问（见「决策模式」）。
- 不把 Claude 自动记忆（memory）作为正式项目规格；正式规格以仓库文档为准。
- 持久性决策必须记录到仓库文档（docs/），不得只保留在会话或记忆中。
- 不主动扩大当前任务范围；发现的范围外问题记 `Follow-up` 或新任务卡（见
  「范围与切片」），**不设「先问用户」这道闸门**——那是一次没必要的往返。
- Creation Workspace 当前仅以
  `docs/ai_video_creation_workspace_requirements.md`、
  `docs/creation_workspace_data_observability_requirements.md`、ADR-0010 和 ADR-0030
  约束实施。只可执行依赖已满足的 TASK-024～033、TASK-008/034～038（完整短剧
  流程）与 TASK-039～040；对应 Proposed
  ADR 未 Accepted 前，不得选择或实现 UI、数据库、Gateway、Action 或最终 schema。
- 完整短剧流程按 TASK-008/034～038 与 ADR-0037～0040 推进；TASK-033 只验收
  WFM1 数据基线，只有 TASK-040 可以宣告两份顶层需求最终完成。

## 实施纪律

- **审查按风险分级触发**，与 AGENTS.md 第 20 条的风险分级表对齐（此前「任何
  feat / fix / refactor 都必须审查」与那张表自相矛盾，矛盾时严格的一条总是赢，
  分级表因此从未真正生效）：

  | 风险 | 审查 | 轮次预算 |
  | --- | --- | --- |
  | 低（CSS/布局/间距/文案/纯展示组合，或修复**不触及**下面中/高任何类别的局部 bug） | **不调用** `codex-review-loop` | 0 |
  | 中（交互/导航/read model/派生视图状态/单层业务逻辑） | 调用 | **1 轮** |
  | 高（持久化/schema/迁移/身份/资产与生成登记/时间线/渲染与文件操作/存储生命周期/付费/并发/安全/Windows 可移植性/跨层合同） | 调用 | **2 轮**（仅当第 2 轮仍报 P1 才允许第 3 轮） |

  **分级取改动触及的最高一档,永远不按 diff 大小或原因是否明显降档。**
  一行修复只要落在持久化、schema/迁移、身份、登记、渲染/文件操作、存储生命周期、
  付费、并发、安全、Windows 可移植性或跨层合同里,就是高风险——「原因很明显」
  只说明修得快,不说明它不会坏事。无法分类按高风险处理。
  （此条为 codex 独立审查 round 1 的唯一 blocking finding:原表述让持久化/安全
  的局部 bug 可以被归为低风险从而绕过强制审查。）
  **P2 修完跑定向测试即可收口，不再开新一轮再审**——
  只有 P1 值得再花一轮。预算耗尽必须显式选择 ship / 只修 P1 / escalate，
  不得静默续轮。（历史代价：TASK-061 13 轮、TASK-062 10 轮，且轮 B4 撤回了
  轮 A4 的修复。）
- **审查不是提交的前置门槛。** 相关测试通过、无已知 P1 即可提交；审查在提交
  之后进行，发现 P1 用后续提交修。此前「审查前不得 commit」的规则会在审查者
  不可用时（codex spend cap + 无 claude fallback）直接死锁整条交付
  —— 实测 2026-08-14 发生过一次：批次 C 代码已完成、测试已绿，卡在拿不到审查者。
- 审查者不可用时**照常提交并推进**，在提交信息与任务卡里如实写明
  「未经独立审查 + 原因」。不假装审过，也不用「测试全绿」冒充独立审查；
  但也不因此停下交付。
- **高风险改动的审查是后移，不是取消。** 未经审查就提交的高风险改动（持久化、
  schema/迁移、身份、安全、并发、付费、破坏性操作、Windows 可移植性、跨层合同）
  必须登记到[待复审清单](docs/design/pending-codex-rereview.md)，审查者恢复后
  立即补审，且 **push / merge / 交接 / 人工验收之前必须完成补审**——与
  [ADR-0068](docs/adr/ADR-0068-continuous-modification-chain.md) 决策 6 同一逻辑：
  可以推迟，不可以跳过。（本条为独立审查的 blocking finding：不加到期约束，
  「审查者不可用」就成了永久移除安全门的通道。）
- **发布闸门是**：用户验收标准满足 + 相关测试通过 + 无未闭合 P1。
  **不是** 零发现 + 全量测试 + 完美架构。P3/P4 默认不阻塞交付，记为 follow-up。
- codex 不可用时该 skill 会自动回退到独立的 claude 会话审查；此时独立性降级，
  必须在报告中如实注明。
- 两者都不可用时（例如 codex 触到 workspace spend cap 且 claude CLI 未安装）：
  **照常提交并推进**，如实写明「未经独立审查 + 原因」；高风险改动登记
  [待复审清单](docs/design/pending-codex-rereview.md)，push / merge 前补审。
  **不得以「测试全绿」冒充独立审查**——如实标注缺口，但不停下交付。
  （2026-08-14 独立审查的 blocking finding：此处原写「**停下并如实报告**」，
  与上面「审查者不可用时照常提交并推进」直接冲突；而本文件的元规则是
  「矛盾时严格的一条总是赢」，因此那条旧文本会让 ADR-0069 决策 6 要消除的
  死锁**原样保留**，本次改动对该场景形同无效。已就地改掉，不是新增一条并存。）
  配套前提：`claude` CLI 必须保持安装（2026-08-14 已装 2.1.232，订阅凭据可用），
  使「两者都不可用」尽量不再发生。
- 连续修改链（[ADR-0068](docs/adr/ADR-0068-continuous-modification-chain.md)）：
  经用户明确授权后，中间批次以定向测试收口并立即独立提交（**审查顺序**按
  [ADR-0069](docs/adr/ADR-0069-review-round-budget-and-parallel-suite.md) 决策 5
  可在提交后进行——这一点修订了 ADR-0068 决策 2 的「审查通过再提交」；
  高风险改动仍须登记待复审清单并在 push / merge 前补审），
  中间提交把 `MOTV_CONTINUOUS_CHAIN=1` **写在提交命令最前面**跳过全量
  （仍跑 ruff 与 diff 检查）；链尾统一跑一次全量 pytest + 全量前端 + ruff +
  最终验收。**该令牌逐次写出，不是环境变量，不得写进 settings 或 profile**
  （gate 是 PreToolUse hook，读的是被拦截的命令文本；见 ADR-0068 决策 7 补记）。
  PowerShell 没有内联赋值语法，写成首行注释 `# MOTV_CONTINUOUS_CHAIN=1` 再换行
  写 `git commit …`。**同一条命令里不得带 push / merge**，否则闸门拒绝提交。
