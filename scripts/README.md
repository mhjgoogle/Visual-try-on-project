# Repository Scripts

Executable repository tooling lives here instead of in the repository root.

| Directory | Ownership |
| --- | --- |
| `launch/` | Human-facing product launchers for supported platforms |

Agent-only tooling remains under `.claude/`; application and library code
remain under `src/`; test configuration and fixtures remain under `tests/`.
