# 交接：`docs/tasks/active/` 的剩余任务（2026-08-23）

产品负责人 2026-08-23：「让另一个对话框做吧。把剩余的 task 让他按顺序做完。
中途不要问我。」

**接手的 Agent 从这里开始读，然后读 [AGENTS.md](../../../AGENTS.md)。**

---

## 0. 先读这一条，它比下面任何一条都重要

**动手做任何一张卡之前，先核实它是不是已经做完了。判据是代码，不是卡片。**

2026-08-23 一天之内，五处状态声明被查出是过期的，**五次全部是文档比事实旧**：

| 声称 | 实际 |
| --- | --- |
| 九张卡「进行中」十天 | 代码 2026-08-13 就已进基线 |
| TASK-052「待开始」 | 四条里两条早做完，**错标签把另外两条真缺陷藏了十天** |
| TASK-087 §1.1「是 3.1/3.2 的硬前置」 | 前置 2026-08-17 就没了（ADR-0071） |
| TASK-083「未开工」 | ADR-A/B/C 全部已定已实施（ADR-0071/0074/0075） |
| TASK-075/076/099「待复审 / 待闸门」 | 补审 2026-08-16 就做完了 |

核实的办法：找迁移函数的署名注释、`grep` 具名交付物、看 `CANVAS_SCHEMA_VERSION`、
看 CI job、看 `git log -S`。**本文件下面写的每一条也一样要核**——它 2026-08-23
是准的，你读到它时可能已经不是。

**这条已经再次命中**：TASK-073 §5.12 说「仍剩 assets / actions / skills 三个控制器」，
但 `src/controllers/` 下 `assetctl.js` 与 `skillctl.js` **今天已经存在**。先核。

---

## 1. 交接时的仓库状态

- `main` = `d57c3ec`，与 `origin/main` 同步，工作树干净。
- **全量绿**（本次交接前跑的那一遍见文末「验证」）。
- **审查欠账为零**：[待复审清单](../active/pending-codex-rereview.md)的「仍然待办」只剩
  POSIX parent-death 一条，状态是**记录**不是欠账。
- 未删的陈旧分支：`feat/wfm1-batch-c`（本地+远端）。那 7 个领先提交已逐个核实
  全在 `origin/main` 内，删除不丢任何提交；`branch -D` 被会话权限层拦下。
  状态记在 `docs/auto-push/changes/wfm1-batch-c.json` 的 `cleanup`。

## 2. 十三项在办，先分成三类

**能做的只有第一类。** 把二三类当成「可以做」会浪费一整轮。

### A. Agent 能独立做完（按建议顺序）

| # | 卡 | 为什么排这个位置 |
| --- | --- | --- |
| 1 | **TASK-056** 应用数据移出仓库 | 自足、小、已核实确实未开始（`projects.json` / `runs.json` 仍在 `mockups/motv-workspace/data/`，`server.py:1518` 的注释还写着「under TASK-056」）。顺带清掉该目录里两个 `runs.json.tmp*` 残留 —— 那是 TASK-087 §6.2 那条 `os.replace` 偶发的落地证据 |
| 2 | **TASK-073 §1.8** `app.js` 拆分收尾 | 有先例可循（已抽出 9 个控制器，工厂 + getter 注入模式见 §5.12），机械、低风险，能减 TASK-087 §3.6.4 的债。**先核实还剩哪几个** |
| 3 | **TASK-083 ADR-D** 项目 / 流程模板 | TASK-083 只剩这一条。卡自己倾向「并入 ADR-0067 的 Skill 包机制而不是另起一套」，但由 ADR 定。技术 ADR 你可以自行 Accept（AGENTS.md §1） |
| 4 | **TASK-087 欠账** 挑能做的做 | §3.5.2 音效需求字段、§3.5.3 音频逐份批准、§4.4 `nonEmpty` 占位词、§4.5 两份别名表无比对、§4.7 `basedOn` 只校验整数、§4.8 漂移基线成员集要派生、§3.6.6~3.6.9 auto-push 工装四条。**每条动手前先核** |
| 5 | **TASK-041** 离线实现 | 状态停在「Accepted，离线实现进行中」（2026-08-07），需要先核实进行到哪 |
| 6 | **TASK-027** part-2b | 长期推迟项，核实依赖是否已满足 |

### B. 需要产品负责人本人，Agent 做不了

| 卡 | 缺什么 |
| --- | --- |
| **TASK-037** WFM2 里程碑 | 证据齐备，等**用户一句话**裁定 PASS（[ADR-0082](../../adr/ADR-0082-no-signoff-gate-on-task-cards.md) 决策 3：里程碑仍归用户，但形式是一句话不是签字栏） |
| **TASK-040** 最终产品里程碑 | 同上 |
| **TASK-074 §1.4** 真实项目八个边界情况 | 卡上原话「**做不到** —— 需要产品负责人用真实素材跑完整一集」 |
| **TASK-072 §1.9 #10** | 需真实素材 |

**不要替他判这四项，也不要为了「清空 active」而把它们标成完成。**

### C. 路线图级，需要自己的规划轮

**TASK-008**（WFM2 字幕/配音/音频，Planned）、**TASK-011** / **TASK-012**（WFM3，
Outline）。它们不是「剩下的小尾巴」，是新里程碑。除非前面都做完了，否则不要开。

### D. 不是任务

**TASK-087** 是**活账**，永远留在 `active/`。它不会被「做完」，只会逐条划掉。

## 3. 这一轮里最该继承的工作方式

- **不要问排序。** AGENTS.md §1 已明写四类永远不问：先做哪个 / 要不要继续 /
  要不要核实 / 把待办端回去让用户挑。**一次做到底，报告写在做完之后。**
  必须停的只有三处：merge 到 main、花钱、会话权限层拒绝。
- **做完就 `git mv` 进 `done/` 并重新生成 `docs/STATUS.md`**
  （`python .claude/tools/gen_docs_status.py`）。`tests/tooling/test_docs_status.py`
  会在忘记时转红（[ADR-0083](../../adr/ADR-0083-docs-partitioned-by-completion.md)）。
- **守卫类改动一律做变异验证**，并且**让回归快速转红而不是挂住套件**。
  TASK-052 的教训：第一版 drip 测试在守卫被拆掉后是**挂 60 秒**，不是红。
- **当心「测试的构造恰好排除了要防的那件事」。** TASK-052 五轮里两次栽在这上面：
  假文件每次返回 1 字节，而真的 `BufferedReader` 会等满 n 字节；以及测试自己没把
  响应 body 读干净却去怀疑服务器。这是 TASK-087 §7 那一族，出现频率极高。
- **审查该跑几轮就跑几轮。** TASK-052 五轮里**四轮各报出一条真 P1**，其中两条是
  **修复自身引入的可用性回归**。单轮收口会把四条全放走。

## 4. 验证（交接前跑的那一遍）

见本文件所在提交的提交信息 —— 内容为两阶段 pytest + 全量前端 + ruff，
以及全仓 md 断链检查。

---

## 5. 执行结果（接手 Agent，2026-08-23 当日）

**A 类六项全部处理完毕**，但其中三项的结论是「早就做完了」或「做不完，原因在下面」。
逐项如实记录 —— 本文件第 0 条那条硬规矩在这一轮里**又命中了四次**。

| # | 卡 | 结果 |
| --- | --- | --- |
| 1 | **TASK-056** | ✅ **做完并收进 `done/`**。注册表进应用数据目录，旧位置只读回退，`--migrate-app-data` 显式迁移；`runs.json.tmp*` 泄漏堵住。**codex 四轮**，轮 2 与轮 3 各报出一条**上一轮修复自身引入的**新机理 P1 |
| 2 | **TASK-073 §1.8** | ✅ **做了第五批**（提示词域 → `promptctl.js`，7803 → 7583）。但**本文件说的「仍剩三个控制器」是过期的**：`assets` / `skills` 早已搬出，`actions` 是判定不搬。真正该报的是 app.js **已经长回基线之上**（详见卡上 §5.16） |
| 3 | **TASK-083 ADR-D** | ✅ **定案** → [ADR-0084](../../adr/ADR-0084-project-flow-template-as-a-package.md)（模板 = Skill 包机制的第二个 kind）。卡收进 `done/`，实现另立 [TASK-105](../../tasks/active/TASK-105-flow-template-first-slice.md) |
| 4 | **TASK-087 欠账** | ✅ **闭合三条**：§3.6.6（`record-sync --hash`）、§3.6.9（`cleanup` 能删「已全在 main 里」的分支 —— 顺带把欠了一整轮的 `feat/wfm1-batch-c` 本地+远端**都删掉了**）、§4.8（复查发现**早已修好**）。新增 §6.4/§6.5/§6.6 三条记录 |
| 5 | **TASK-041** | ⚠️ **核实后拆成两段**：增量 1、2 **已完成**（28 项测试全绿，`gateway.js` 早已不是 stub）；**第 4 项离线搭建未做**（`examples/projects/` 下没有 catalog 目录）；**增量 3 要花钱 → B 类**，Agent 不得推进。卡上已改写 |
| 6 | **TASK-027** | ⚠️ **推迟的理由已不成立**（ADR-0038 已 Accepted，TASK-035/039 都在 `done/`），但**动手前要先定 `workspace_shell` 的去留** —— TASK-083 §5.1 要求过、TASK-103 声明会做，全仓 grep **找不到结论**。卡上已写明 |

### 这一轮新查出的过期状态（第 0 条又命中四次）

| 声称 | 实际 |
| --- | --- |
| 本文件：TASK-073 §1.8「仍剩 assets / actions / skills」 | 两个早已搬出，一个判定不搬；真问题是 app.js 长回去了 |
| TASK-087 §4.8「漂移基线只覆盖 20 / 22 个包」 | 基线 26 条、成员集合已从目录派生，**早就是本条要求的形状** |
| `project-context.md`「写侧保持 stub / 不接真实 Command Gateway」 | 早就不成立：`gateway.js` 是真实两步提交，Studio 注册了五个真命令。已订正 |
| TASK-027「等 ADR-0038 落地」 | ADR-0038 已 Accepted，TASK-035/039 都在 `done/` |

### 顺带做的两件本文件没点名的事

- **全仓 md 断链守卫**（`tests/tooling/test_docs_links.py`）。ADR-0083 让「做完就
  `git mv`」成为流程，而那个动作**每次都会打断入链**，此前没有任何东西接住：
  本轮移两张卡就断了 9 条，同一次扫描还翻出 **8 条早就断了的**。已全部修好，
  全仓断链归零，并加了守卫 + 变异验证。
- **`docs/design/active/remaining-roadmap-design-report.md` 收进 `done/`** ——
  它自己开头就写着「历史文档，不再作为当前状态事实来源」。

### 没做的，和为什么

- **B 类四项一律没碰**（TASK-037 / 040 / 074 §1.4 / 072 §1.9 #10），按本文件要求。
- **TASK-041 增量 3**：要花钱，AGENTS.md §1 里唯一必须问的那件事。
- **C 类三张路线图卡**（TASK-008 / 011 / 012）：本文件说「除非前面都做完了否则不要开」，
  而前面并没有都做完（见上表第 5、6 项）。
- **一次会话权限层拒绝，如实记一次**：测试第一版曾把五个 pytest 临时项目写进开发机
  真实的 `%LOCALAPPDATA%\motv\projects.json`（缺陷已修，见 TASK-056 卡末）。
  那个目录**需要用户自己删掉** —— 删仓库之外的路径被会话权限层拦下了。
  内容全是指向已消失的临时目录的垃圾条目，后端本来也会把找不到的项目丢掉，
  所以不删也无害，只是脏。
