# 待补 codex 复审清单（活账）

- 状态：**活账**（不是一次性交付）。**当前未闭合条目：0**（最后核对 2026-08-26）
- 依据：[ADR-0068 补记](../../adr/ADR-0068-continuous-modification-chain.md#补记降级审查模式2026-08-13--2026-08-18) ·
  AGENTS.md §20「该审而未审的改动是后移审查，不是取消」
- 历史（2026-08-13 – 2026-08-23 的全部已闭合条目，一字未改）：
  [codex 补审历史](../done/codex-rereview-history-2026-08.md)

## 规则（不变）

**本清单有未闭合条目时，不得 push / merge / 交接 / 宣告最终验收。**

每一个在降级审查模式下通过的检查点，在下表加一行。补完复审后**划掉该行并注明
复审提交，不要删除行** —— 删掉就看不出这段时间发生过什么。行数多到读不动时，
把**已闭合**的整段移进上面那份历史文件（ADR-0087 决策 1：已完成记录移出默认
上下文，历史不删）。

`merge` 前的查法：读**本文件**的下一节。查到有未闭合条目 → 先补审，再
`set-merge-gate`；`premerge-sync` 的 `--ledger-checked` 声明的就是这一步。

## 待复审（未闭合）

| 提交 / 范围 | 为什么在表上 | 状态 |
| --- | --- | --- |
| ~~`248319e` `4bf8053`（TASK-122）· `54b27db` `180d09c`（TASK-126）~~ | 四个提交在提交信息里自称「未经独立审查」，**当时没有登记到本表** —— 这正是本表要防的那件事。2026-09-05 清点 `main..HEAD` 时才发现：清单写着「0 条未闭合」，而分支上躺着四条自称没审的改动。两者对不上的时候，**分支是事实，清单是记录** | **已闭合** · 2026-09-05 分三块补审：块 2a（数据模型 + 动作表）15 轮修 23 条 · 块 2b（壳层）· 块 1（服务端与工装）。6 轮之后全为真 codex，独立性未降级。报告见任务卡 TASK-126 |

> **当时判「codex 不可用」的理由是错的，而且错了两层。**
>
> 第一层：停用的是**产品内的创作执行器**（ADR-0042 的注入面），那与**仓库里的
> 代码审查**是两回事。把前者的策略当成后者的可用性，等于凭一个不成立的理由跳过审查。
>
> 第二层（2026-09-05 查实）：即便真去跑，`codex-cli 0.147.0` 也确实失败 —— 但原因
> 是**版本旧**：`~/.codex/config.toml` 指定的 `gpt-6-astra` 老 CLI 用不了，API 直接
> 回绝「requires a newer version of Codex」。升级到 **0.153.4** 后立即可用。
> 另一个会话（TASK-131）用它取到真实验收证据，独立证实了同一件事。
>
> **所以：以后遇到「codex 不可用」，先 `codex --version` 并实跑一次最小推理，
> 再决定要不要回退。** 不实跑就断言不可用，是这次四个提交漏审的直接原因。
>
> **一条自查（2026-09-05）**：这条闭合我在只做完块 2a 时就写了，而那四个提交的
> `server.py` 与 `production.js` 改动在块 1 / 2b 里，当时还没审。**闭合写早了** ——
> 后来补齐了三块才让这句话成立。本表要防的就是「记录跑在事实前面」，我自己也犯了
> 一次；写闭合之前先问「我实际审完的范围，是不是等于这条记的范围」。
>
> 补审的收益是可量化的：**升级后第一轮 codex 就抓到一条五轮 claude 全没看见的
> 数据丢失**（恢复旧版本会改写并拽出回收区里的行，较新内容永久丢失）。
> 同模型审同模型会跟着实施者一起相信同一个心智模型。

## 非欠账记录（不阻塞任何闸门）

| 项 | 说明 |
| --- | --- |
| POSIX 缺 parent-death 机制 | Windows 由 Job Object 兜底；POSIX 需 `PR_SET_PDEATHSIG` 且对孙进程无效。**是记录不是审查欠账**，正账在 [TASK-087 §2.3](../../tasks/active/TASK-087-followup-ledger.md) |
