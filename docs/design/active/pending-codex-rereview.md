# 待补 codex 复审清单（活账）

- 状态：**活账**（不是一次性交付）。**当前未闭合条目：0**（最后核对 2026-08-26）
- 依据：[ADR-0068 补记](../../adr/ADR-0068-continuous-modification-chain.md#补记降级审查模式2026-08-13--2026-08-18) ·
  AGENTS.md §20「该审而未审的改动是后移审查，不是取消」
- 历史（2026-08-13 – 2026-08-23 的全部已闭合条目，一字未改）：
  [codex 补审历史](../done/codex-rereview-history-2026-08.md)

## 规则（不变）

**本清单有未闭合条目时，不得 push / merge / 交接 / 宣告最终验收。**

每一个在降级审查模式下通过的检查点，在下表加一行。补完复审后**划掉该行并注明
复审提交，不要删除行** —— 删掉就看不出这段时间发生过什么。行数多到读不动时，
把**已闭合**的整段移进上面那份历史文件（ADR-0087 决策 1：已完成记录移出默认
上下文，历史不删）。

`merge` 前的查法：读**本文件**的下一节。查到有未闭合条目 → 先补审，再
`set-merge-gate`；`premerge-sync` 的 `--ledger-checked` 声明的就是这一步。

## 待复审（未闭合）

| 提交 / 范围 | 为什么在表上 | 状态 |
| --- | --- | --- |
| —— | —— | **空。** 2026-08-26 核对：历史文件里的全部条目均已闭合 |

## 非欠账记录（不阻塞任何闸门）

| 项 | 说明 |
| --- | --- |
| POSIX 缺 parent-death 机制 | Windows 由 Job Object 兜底；POSIX 需 `PR_SET_PDEATHSIG` 且对孙进程无效。**是记录不是审查欠账**，正账在 [TASK-087 §2.3](../../tasks/active/TASK-087-followup-ledger.md) |
