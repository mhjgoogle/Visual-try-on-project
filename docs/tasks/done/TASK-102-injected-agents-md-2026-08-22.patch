# TASK-102 注入事件证据：AGENTS.md 的未提交外来修改（2026-08-22，已还原未采纳）
# 注：为通过仓库空白检查，各行行尾空白已剥离、EOF 空行已收敛；除此外与原 diff 逐字一致。
diff --git a/AGENTS.md b/AGENTS.md
index 73d1132..711ec2d 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -7,9 +7,8 @@ Agent 之间只通过仓库中的文档、代码和 Git 状态共享上下文，

 ### 长期目标

-构建一个 AI 视频 / AI 短剧生产工作流（权威开发环境为 WSL2 Ubuntu + VS Code；
-自 [ADR-0049](docs/adr/ADR-0049-native-windows-run-and-test-target.md) 起，原生
-Windows 为受支持的**运行+测试**目标），覆盖：
+构建一个 AI 视频 / AI 短剧生产工作流（权威开发/构建/CI/Agent 环境为原生
+Windows + NTFS；Ubuntu / WSL2 与 Linux CI runner 为受支持目标），覆盖：

 故事构思 → 结构化剧本 → 场景与镜头拆分 → 人物/场景/道具资产管理 → 图片生成
 → 视频生成 → 配音、音效和字幕 → FFmpeg 合成 → 质量检查
@@ -88,6 +87,22 @@ L0–S7 阶段/步骤的逻辑输入输出基线见
 Skill 只产生提案，**不得定稿、锁定、付费或导出**；加载或校验失败一律 fail-closed。
 实施见 [TASK-075](docs/tasks/done/TASK-075-product-skill-package.md)。

+### Agent Skill（跨 Agent 工作流资产）
+
+本仓库另有一类 **Agent Skill**：`.claude/skills/<name>/SKILL.md` 中的可复用
+开发工作流。它与上面的产品 Skill 无关，不进入 Studio 运行时，也不使用
+`manifest.json + prompt.md + output.schema.json` 三件套。
+
+`.claude/skills/` 是历史命名，不等于 Claude 专属。Claude 可从此路径自动发现；
+其他 Agent 必须在请求触发其描述时读取相应 `SKILL.md` 并遵守其流程。支持该格式的
+工具也可自动发现该目录。不得在 `.github/` 复制另一份 Skill 真相。
+
+当前公共入口为：开发改动先读 `dev-workflow`；需要独立审查时读
+`codex-review-loop`（名称保留兼容，流程对 Agent 中立）；受管 Change 的 Git
+操作读 `auto-push`；任务收口的 Skill 反馈读 `skill-evolution`。Skill 与本文件或
+Accepted ADR 冲突时，以本文件和 ADR 为准；Skill 的核心行为变更仍按 ADR-0078
+先经用户批准。
+
 ## 2. 技术与环境约束

 1. Python 是主要开发语言。
@@ -111,8 +126,10 @@ Skill 只产生提案，**不得定稿、锁定、付费或导出**；加载或
    149,986 行 vs 1,918 行）：
    `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.autocrlf GIT_CONFIG_VALUE_0=true`。
    不改共享配置。
-6. 所有外部工具（ffmpeg/ffprobe/piper/claude/codex/node）一律经 `shutil.which`
-   解析、**失败即 fail-closed**，不得裸名调用（ADR-0049 / ADR-0062 决策 2）。
+6. 产品与流水线代码中的外部工具（ffmpeg/ffprobe/piper/claude/codex/node）一律经
+   `shutil.which` 解析、**失败即 fail-closed**，不得裸名调用（ADR-0049 /
+   ADR-0062 决策 2）。Agent 工装可用宿主 shell 的等价解析（PowerShell
+   `Get-Command`、POSIX `command -v` 或项目 venv 解释器），也必须 fail-closed。
    Windows 权威下这条更容易被触发且**必须如实报告**：安装器改了 PATH，但早于安装
    启动的进程（agent shell、commit gate 的 hook）看不到新 PATH——正确处理是重启会话，
    不是让代码去猜路径。
@@ -145,7 +162,7 @@ pause/cancel/skip 状态。
 14. 每个开发任务（`docs/tasks/TASK-*.md`）只能有一个实施 Agent。
 15. 另一个 Agent 只能作为独立审查者，不得在同一任务上并行修改代码。
     审查范围必须**明确限定为本次 diff 及它能影响的不变量**，不得每轮重新审查
-    整个架构或整个仓库。审查者判断的是**实际风险**（正确性、回归、状态一致性、
+    整个架构或整个仓库。审查者判断的是实际影响（正确性、回归、状态一致性、
     竞态、身份），不是「还能不能继续优化」；只报 P3/P4 时不得阻塞交付。
 16. 修改前必须检查 `git status` 和相关文件的当前内容，确认没有他人未提交的改动被破坏。
 17. 不修改当前任务范围之外的代码；发现范围外问题时记录到文档或新任务，不顺手修改。
@@ -154,30 +171,32 @@ pause/cancel/skip 状态。
 ## 5. 质量规则

 19. 新功能必须有测试。
-20. 测试按**风险分级**运行，规模与改动风险匹配，不是每次改动都跑全量：
-
-    | 风险 | 改动内容 | 运行 |
-    | --- | --- | --- |
-    | 低 | CSS、布局、间距、排版、纯展示组件组合、静态文案 | 相关前端测试 + 手动 smoke |
-    | 中 | 交互、导航、read model、派生视图状态、筛选/排序/选择 | 受影响的前端/单元测试 + 必要时定向 pytest |
-    | 高 | 持久化、schema/迁移、资产登记、生成登记、时间线、渲染/文件操作、存储生命周期、Windows 可移植性/安全 | **全量 pytest + 全量前端 + ruff + Codex 独立审查** |
-
-    本地 commit gate 按 [ADR-0060](docs/adr/ADR-0060-risk-based-local-commit-gate.md)
-    的保守 allowlist 选择检查；高风险与无法分类的改动必须跑全量。全量测试仍是
-    有意义的 checkpoint、合并前 CI 与高风险代码变更的硬门槛。
-
-    **审查同样按同一张表分级，并且有轮次预算**（低 = 不审 / 中 = 1 轮 /
-    高 = 2 轮，仅当第 2 轮仍报 P1 才允许第 3 轮）。**低 / 中风险的 P2 修完跑定向
-    测试即可收口，不再开一轮再审；高风险的 P2 修复必须用掉剩余那一轮复审**
-    （ADR-0069 决策 3——此处原写成无条件，与该决策冲突，2026-08-16 codex 跨模型
-    复审报为 blocking，已就地限定）——只有 P1 值得再花一轮；每轮约 6–10 分钟 ×
-    审查者数量，而审查者永远能找到更窄的 P2 变体，所以 P2 触发再审在结构上
-    不收敛。历史代价：TASK-061 13 轮、TASK-062 10 轮，其中轮 B4 撤回了轮 A4
-    的修复（净负值）。预算耗尽必须显式选择 **ship / 只修 P1 / escalate**，
-    不得静默续轮。
-
-    **发布闸门 = 用户验收标准满足 + 相关测试通过 + 无未闭合 P1**，
-    而不是零发现 + 全量测试 + 完美架构。`VERDICT: pass` 不是闸门。
+20. **验证按测试所有权与影响范围，不做风险分级。** 每个日常提交至少运行：
+
+    | 影响范围 | 必须验证 |
+    | --- | --- |
+    | 单一功能/模块 | 该域测试 + 直接消费者测试 |
+    | API、schema 或跨层合同 | 合同测试 + 所有直接调用方测试 |
+    | UI/前端 | 受影响的行为测试 + 手动 smoke |
+    | shared/core | 直接依赖域的测试并集 |
+    | Bug | 回归测试 + 上述对应验证 |
+
+    `commit_gate_policy.py` 维护已知路径到测试域的映射；它没有映射不表示可以少测。
+    先补足影响分析并扩大到可确定的共同测试域；仍无法界定时，fail-closed 到完整
+    集成套件，并把缺失归属记入 TASK-102。不得因“高风险”标签自动把每次独立切片
+    升为全量。
+
+    **完整集成检查点**为：CI、merge 到 main、跨域或依赖图不清的改动完成整合、
+    用户要求的发布或交接前。它运行完整前端套件、ruff 以及两阶段 pytest。
+
+    **独立审查按影响触发，不按档位触发。** 行为、持久化、合同、安全、并发、
+    文件操作或跨域改动做一次独立审查；纯文档与纯展示改动可不审。P1 必须修复并
+    复审一次；P2 修复后跑归属测试即可，不因 P2 开新一轮；P3/P4 记录 Follow-up。
+    审查者不可用时可如实标记后提交，但不得把测试通过称为独立审查；尚待完成的
+    必要审查必须在 merge、交接或人工验收前补齐。
+
+    **交付闸门 = 用户验收标准满足 + 相关测试通过 + 无未闭合 P1**，不是零发现、
+    全量测试或完美架构。`VERDICT: pass` 不是独立闸门。

     **全量 pytest 改为两阶段并行跑**：
