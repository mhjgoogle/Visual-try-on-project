# TASK-011：LocalVideoProvider（阶段 8）

> **状态：Outline（可选 WFM3 升级）。**
>
> **2026-08-24 订正：卡住它的不是「等用户裁决」，是「WFM3 还没排上」。**
> 原文写「本地模型选型与硬件约束须用户裁决」—— 那是旧规则留下的闸。按现行
> AGENTS.md §1，**依赖选型属于「一切技术决策」，由实施 Agent 自己定并写进
> `docs/`**；「WSL2 显存约束」根本不是决策，是一个**量一下就知道的事实**；
> 「权重存放位置」同样是技术决策（且第 23 条已经规定不进 Git）。
>
> 产品负责人 2026-08-24：「用不花钱的方向就好了」—— 本地模型**正是**不花钱那
> 条路（对照 TASK-012 依赖的云 Provider）。所以方向上没有分歧。
>
> **那为什么现在不做**：AGENTS.md §2「同一时间最多推进 1 个主要用户需求」
> 与「技术基础只做 Minimum Necessary Foundation」。TASK-074 还开着（§1.4 等
> 真实项目验收 → §1.5 删除 → §1.6 收口），此时铺开一条 WFM3 的新 Provider
> 正是那一节要防的「基础工作开始膨胀」。**这是排期，不是闸门** —— 真要做的
> 那天，上面三条我自己定完写进 ADR 就能开工，不必回来问。

## 正式名称

Local Video Model Provider

## 业务目标

调用本地部署的视频生成模型实现 `VideoProvider` 接口，媒体只输出
到编排器指定的 staging 路径，上层流程零改动。

## 待定项（**实施 Agent 自己定**，不是用户裁决）

> 标题原为「阻塞项（用户裁决）」，2026-08-24 按 AGENTS.md §1 订正：
> 下面三条都在「一切技术决策」之内。列在这里是**开工前要定**，
> 不是**要问人**。

1. 本地模型选型与运行方式（进程内 / 独立推理服务 / 队列）；
2. WSL2 GPU/显存资源约束确认；
3. 模型权重存放位置（不进 Git）。

## 边界合同（已可锁定的部分）

- 实现 TASK-003 契约；submit 触发本地推理，poll 报告队列/推理
  进度，媒体只写编排器提供的 staging 路径（architecture.md §5）；
- 排队、显存约束、失败重试的类型化错误与状态映射；
- 推理进程管理不得污染核心库（独立模块、显式生命周期）；
- 新增 `providers/local_<model>.py` + 测试（推理全部打桩；真实
  推理冒烟 opt-in）。

## 依赖

TASK-003/004（已交付）；TASK-005/006/007（端到端验证链路）；
用户裁决 1–3。与现行 TASK-016/017 云 Provider 路线互相独立；最终产品验收
不以本地 Provider 为硬依赖，只有用户选择本地路线时才进入实施。

## 当前状态

Optional WFM3 upgrade — blocked on user product decisions
（模型/硬件/存放）；不计入 WFM2/WFM3 最小验收门槛。
