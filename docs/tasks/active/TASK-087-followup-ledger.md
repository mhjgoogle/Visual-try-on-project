# TASK-087：Follow-up 总账 —— 把散在九张卡里的欠账收成一处

- 状态：**活账**（不是一次性交付；每条闭合时就地划掉并注明提交，**不删行**）
- 负责 Agent：无固定实施 Agent —— 这是一份**登记**，每条各自开工时才归属一个 Agent
- 起因：TASK-077～086 九张卡各自留了 Follow-up，散在六个文件里。
  下一个人看不全，于是同一条会被重复发现（本轮已经发生过：
  `ui-gap-analysis.md` 里的 38 被 TASK-078 重新查了一遍）
- 依据：各卡 Follow-up 段 + [待复审清单](../../design/active/pending-codex-rereview.md) +
  [ui-correction-plan.md](../../../src/ui-gap-audit/reports/ui-correction-plan.md)

---

## 0. 这张卡不做事，它防止遗忘

**它不改任何代码。** 唯一的功能是：让「还欠什么」有一个地方能一眼看完，
并且每条都带**风险档**和**为什么还没做** —— 后者比前者重要，因为
「忘了」和「刻意押后」在清单上长得一样，而处理方式完全不同。

排序不是优先级，是**分组**：按「拦不拦得住下一步」分。

---

## 1. 拦住别的工作的（先做这些）

| # | 欠账 | 来源 | 风险 | 为什么还没做 |
| --- | --- | --- | --- | --- |
| 1.1 | ~~**参考数组化 + `{{Image N}}` + `ProviderRequest` 多图契约**~~ **已闭合（2026-08-23 复查）** | GAP-27/28 · TASK-083 ADR-A | —— | 决策 [ADR-0071](../../adr/ADR-0071-reference-inputs-as-an-ordered-set.md)（Accepted 2026-08-17，付费口径方案 C 由产品负责人拍板），实现 TASK-097 批次 0/2。四点全落：`refset.js` 有序 ordinal、`{{Image N}}`、`ProviderRequest.reference_images` + catalog 按 model 声明 `max_images`、能力不足 **fail-closed 两道**。~~是 1.2 / 3.1 / 3.2 的硬前置~~ —— **这道前置已经不存在了**，它却一直挂在这里，把 3.1 / 3.2 也标成「等 1.1」 |
| 1.2 | ~~**评价 / 反馈 / 行动闭环接进 Studio Gateway**~~ **评价一半已闭合（TASK-103 批次 B · `6bb6751`）；反馈一半仍开着** | GAP-05 · TASK-079 §7 · TASK-083 §5.1 | **高**（跨层写路径） | 四条 LOW-risk 命令已注册进 Studio Gateway（共享缝 `register_creative_loop_commands`，规格不复制），审片页的「✓ 通过 / 撤销通过」经网关落核心并把回执记在 `reviewsDoc.coreSync` 上如实显示。**没做的那一半**：`create-feedback` 至今没有调用方 —— 不是忘了接，是**界面上根本没有「提出审片问题」这个动作**（`workflow/review.js` 的 `issue()` 在整个 `src/` 里零调用方）。本卡曾一度写了个 `feedbackFor` 构造器并配测试，codex 轮 1 判为 P1（导出了、测了、没人用，且让「闭环已接通」这句话看起来成立），已删除。见新增的 §5.9 |

## 2. 安全 / 质量门（能力边界，说清比堵死重要）

| # | 欠账 | 来源 | 风险 | 为什么还没做 |
| --- | --- | --- | --- | --- |
| 2.1 | **仓库内 `pre-commit` git hook** | TASK-085 §6 | 高 | 唯一能堵死 `eval` / `bash -c` / 变量间接 / 子进程的那一层——它在 git 进程内。TASK-085 把边界从「无意即可绕过」抬到「必须故意绕过」，**没有**抬到「不可绕过」，且这一点已写进代码注释并钉成可执行断言 |
| 2.2 | Skill 包的 **TOCTOU 与硬链接** | TASK-084 项 4 | 高 | **刻意不修**：两者都需要 `studio/skills` 的写权限，而有该权限者可直接把包放进项目——修它买不到安全性。已写进代码注释 |
| 2.3 | POSIX 缺 parent-death 机制 | 待复审清单 | 中 | Windows 由 Job Object 兜底；POSIX 需 `PR_SET_PDEATHSIG`，且**对孙进程无效**——修一半的价值待评估 |
| 2.4 | 审查脚本默认审整个未提交 diff | TASK-084 §7 · TASK-085 | 低 | 并行会话时会把对方在制品扫进 diff（本轮浪费过 4 条 finding）。`REVIEW_EXTRA_EXCLUDES` 是权宜；根治是**默认只审本次意图范围** |
| 2.5 | ~~**`Test-BinaryFile` 把「读不出来」当成「是二进制」并静默跳过**~~ **已闭合（2026-08-23）** | TASK-052 §2.4（2026-08-23 复查确认仍在） | 中 | `run-review.ps1:330` 的 `catch { return $true }`。被 ACL / 共享锁锁住的源码文件**没有被审查**却让整轮拿到 `pass` —— 这是审查门自己的完整性缺口，和 §7 那条「守卫看起来加了、其实没接上」同一族。读取失败应当报错，不是当二进制 |
| 2.6 | ~~**`_discard_body()` 有字节上界、没有时间上界**~~ **已闭合（2026-08-23）** | TASK-052 §2.2（2026-08-23 复查确认只做了一半） | 中 | `src/workspace_shell/server.py:92`。`_MAX_BODY_BYTES` 拦得住「谎报一个巨大 Content-Length」，拦不住「声明一个合法值再慢速发送」—— slowloris 类占线程。缺的是 socket deadline / 最大读取时长 |

> **2.5 / 2.6 已于 2026-08-23 闭合** —— TASK-052 本轮实施，codex 跨模型 5 轮
> （轮 1–4 各报 P1，轮 5 pass）。2.2 那条最终需要**三道界**才真的关上：每次读的
> 停顿界、body 的总时限（且必须 `read1()`），以及一个只盖「等待对端」的连接级
> 看门狗（请求行与头部在 `http.server` 内部读，应用执行则被排除在外）。
>
> **原出处**：TASK-052 那张卡原本整张标着「待开始」，2026-08-23 逐条复查发现
> 它的四条里 **2.1 / 2.3 早已被 TASK-085 等做掉**，只剩这两条。登记在此是为了「还欠
> 什么」能一眼看完；**细节与证据不复制**，在 TASK-052 各小节的开头。

## 3. 领域能力（需 ADR）—— ~~等 1.1~~ **1.1 已于 2026-08-23 闭合，这一节不再被它挡住**

| # | 欠账 | 来源 | 风险 |
| --- | --- | --- | --- |
| 3.1 | ~~**从一张图创建角色**~~ **已闭合（2026-08-23 复查）** —— [ADR-0074](../../adr/ADR-0074-character-from-image.md) Accepted 2026-08-18，实现 TASK-097 批次 3；入口在镜头画布「以此生成 →→ 从图创建角色」。**角色原型库仍未做**（按 ADR 自己的建议：等使用数据） | GAP-17 · TASK-083 ADR-C | —— |
| 3.2 | ~~运镜~~ **已闭合** / **风格 · 特效预设库仍未做** | GAP-16 · TASK-083 ADR-B | 中 · ADR | 运镜由 [ADR-0075](../../adr/ADR-0075-camera-motion-presets.md)（Accepted 2026-08-18，TASK-097 批次 3）解决：预设是内置常量、应用即复制文本、落到镜头上与预设脱钩，于是「改了预设、已用过它的镜头怎么办」这个版本语义问题被设计掉了。**风格 / 特效是 ADR-0075 显式留下的**，不是遗漏 —— 它们**会进生成**（风格参考图是 `model-input`），需要资产与版本绑定，与文本模板不同类，要单独一个 ADR |
| 3.3 | 项目 / 流程模板 | GAP-21 · TASK-083 ADR-D | 中 · ADR，可能并入 ADR-0067 |
| 3.4 | 「同参数重放」需要 **redo 任务语义** | TASK-079 §7 | 高 | Gateway 对同一 `task_id` 已有付费操作会拒绝并要求建 redo。真正的原样重跑得先定义这个领域操作 |

## 3.5 TASK-097 剧集制作链交来的六条（2026-08-20 链尾登记）

**这一批的共同点是：每一条都已经在界面上如实说出来了**，登记在这里是为了
「说出来」不被当成「做完了」。

| # | 欠账 | 来源 | 风险 | 它今天挡住什么 |
| --- | --- | --- | --- | --- |
| 3.5.1 | **Gateway 没有批量预检命令** | TASK-097 批次 4E · TASK-095 §7.6 | 高（付费） | **阻塞 TASK-095 §3 的「preflight 给总额，一次确认整批」那一条。** 今天 ⑥ 批量生视频只能如实说给不出总额，并把「逐镜生成，每镜自己走 ADR-0041 两步确认」摆在同一块上。**不得**用逐镜价相加代替（那就是被禁的「单价 ×N」），**不得**补 0 |
| ~~3.5.2~~ | ~~**镜头上没有「这一镜需要什么音效」这个字段**~~ **已闭合（2026-08-24）** | TASK-097 批次 5A · TASK-096 §2.2 订正 | 中（schema 加法 + 分镜表列） | 新增加法字段 `sfxNote`（分镜表「音效需求」列，可写），**口径与 `dialogue` 完全一致**：写了→要做、空着→`null`（没人写下来，**不是不需要** —— 「不需要」要标为跳过，那是会被存下来的人的决定）。`soundNeed(shot,'sfx')` 因此不再无条件返回 null，逐镜质检对音效不再永远答「无法判定」。**「音效需求」与既有的「音效」计数列并排**，是「要什么」与「有什么」两件事 —— 后者没错，本条欠账说的是前者没有地方写。登记进 `ADDITIVE_SHOT_FIELDS`（漏登记时既有守卫当场变红 —— 那正是 TASK-055 §5「保存镜头静默丢失景别/角度/情绪」留下的规矩）。三条旧文案测试钉的是被本次取代的旧事实（「没有地方写」），已更新。测试 6 条含完整保存往返；变异验证已做。**未做**：拆分镜 Skill 尚未产出这个字段 —— 那是 Skill 包的一次修订，另记 |
| ~~3.5.3~~ | ~~**音频没有逐份批准**~~ **已闭合（2026-08-24）** | TASK-097 批次 5A | 中 | **不需要新 schema** —— `approvedFor` 本来就是绑产物的（`shotstage.js` 那行注释：「绑产物，不绑 stage」），缺的只是「多份时算谁」。答案照抄这个模块自己已经用在`present` 上的规矩：**每一份都通过才算通过**（三条音效里丢了一条不算做完，那三条里有一条没过当然也不算通过）。`audioEvidence` 新增 `assetIds`；`assetId` 保持单份时才给 —— 多份时确实不存在「那一个」。两个坑显式堵上：**空集不算全通过**（`[].every` 恒真，会把「一条都没有」说成「全通过了」）；**有片段但没有 assetId 时整步不通过**（它没人能批准，当作不存在会让一条没人看过的音频混进通过里）。后一条是我第一版**只写了注释没有实现**、测试也只描述没断言 —— 补齐了。测试 4 条 |
| ~~3.5.4~~ | ~~**逐镜时长测量走的是重型端点**~~ **已闭合（2026-08-24）** | TASK-097 批次 5B | 低 | 逐镜质检的 `measure` 已切到 `GET /api/projects/<p>/media-audit?measure=<file>`（ffprobe-only，不解码）。**整段成片的交付质检仍走 `/api/delivery/probe`** —— 它真的需要那次解码（ebur128 + blackdetect），不是漏改。切换不是等价替换：轻端点把失败分成**五种具名状态**（`ok`/`bad_name`/`not_found`/`no_ffprobe`/`unreadable`），每一种把创作者送到不同的下一步（装 ffprobe / 找文件 / 改文件名），所以逐种给了话说，没压成一句「探测失败」。新增跨语言合同测试 `tests/contract/test_media_audit_states_task087.py`：服务端加一种状态而前端没跟上时会红（**两个方向都红**，且带一条「扫描面非空」的自检）—— 否则那一种会静默落到兜底文案，恰好把这个端点专门保留的区分丢掉。变异验证两次全红 |
| 3.5.5 | **一致性判据只覆盖「已绑定」的参考** | TASK-097 批次 5B | 低 | 「`@角色` 提到了但没绑定设定图」属于**绑定缺口**，不属于一致性；今天前者由第 ② 步/画布负责，后者由质检负责。两者都在界面上说得出，但没有一处把它们并起来看 |
| 3.5.6 | **`shotwork` 这一页的 section 与模块表同名键混淆** | TASK-097 批次 4F / 4E / 5B（**同一处三次**） | 低（但反复付成本） | 这一页自己切 section，而模块渲染表里另有 `video:` / `audio:` / `edit:` 等历史键。挂错了不会报错 —— 屏幕上什么都不出现。三次都靠**看真实屏幕**抓到。真正的修法是让「一个内容挂在哪」只有一处答案，不是每次记得多看一眼 |

## 3.6 TASK-102 解耦重构交来的四条架构耦合（2026-08-22 链尾登记）

这一批的共同点是：**测试层的联动已经解掉了，代码层的边界错位还在。**
本卡刻意只动测试归属与文档（不改产品行为），下面四条是它如实留下的。

| # | 欠账 | 来源 | 风险 | 它今天挡住什么 |
| --- | --- | --- | --- | --- |
| 3.6.1 | **Studio 的产品后端住在 `mockups/motv-workspace/`** | TASK-102 盘点 · ADR-0077 决策 6 已登记 | 高（持久化 + 身份 + 付费路径都在这一个 358KB 单体里） | 「mockup」这个目录名与它承担的正式能力不符：启动器把它当产品入口启动，46 个 pytest 把它当产品测。本卡已把测试归属理清到 `tests/studio/`，**代码一行未动**。要一个「Studio 正式应用化」任务一次性处理包入口、导入路径与数据路径——不得趁别的任务零散搬移（ADR-0077 决策 6 原文） |
| 3.6.2 | **`src` 内部 import 环** | TASK-102 盘点（自动统计 import 方向） | 中 | `app → planning` 且 `planning → app`（应用层与领域层双向）；`learning → workspace → app → …` 长链。核心库对 mockups 是**零依赖**（这一侧干净），但内部方向不干净：动 app 会波及领域层，反之亦然。本卡 OUT OF SCOPE |
| 3.6.3 | **L0–S7 合同手抄三份** | TASK-102 盘点 | 中 | `docs/design/workflow-stage-step-io-contract.md`（权威）↔ `src/…/workspace/io_contract.py`（801 行，自称 transcription）↔ `mockups/…/src/workflow/contract.js`（78 行，自称 mirrors）。改合同要同时改三处，漏一处就静默不一致。反例参照：`product-skills/skill-inputs.json` 是**单一来源**被前后端同时读，形状健康 |
| 3.6.4 | **前端入口 `app.js` 不可测** | TASK-102 批次 E | 低（已缓解） | 它是入口编排文件，`.test.mjs` 无法 import，所以它的架构不变量只能从**源码文本**侧断言。批次 E 已把这些守卫从 19 个测试文件收敛到 `tests/contract/` 一个文件（改 app.js 只碰一处），但断言的仍是写法而非性质。根治要拆 app.js 的编排层——属前端重构，另立卡 |
| 3.6.5 | **auto-push 的 `scope_violation` 是过期快照，且无重算通道** | TASK-102 merge 时实测 | 中（会**无理由拦住 merge**） | `record-commit` 按**当时**申报的 `paths` 算 `scope_violation` 并存进清单；后续 `task-ready` 覆盖 `paths` 之后，旧快照与新申报不再一致，而 `merge` 只看快照 → `BLOCKED_SCOPE`。本次两条提交的 13 + 2 个「越界」文件其实全在最终申报范围内，用 `autopush._matches` 按当前申报重算后为 0。`stage` 有 `--allow-wide` 这个出口，`record-commit`／`merge` 侧没有对应物。修法建议：`merge` 前按当前 `paths` 重算，或加一个 `recheck-scope` 子命令；顺带考虑 `task-ready` 覆盖 `paths` 时就地重算历史记录。归 TASK-101 / auto-push skill |

### 3.6b TASK-101/103/104 三条 Change 合并时暴露的四条（2026-08-23 登记）

共同点与 3.6.5 同族：**登记表能记的和工具能读的对不上**，于是闸门在真实事实
成立的情况下拦住合并 —— 每一条都靠人工核实事实后手工补，而不是工具自己收敛。

| # | 欠账 | 来源 | 风险 | 它今天挡住什么 |
| --- | --- | --- | --- | --- |
| ~~3.6.6~~ | ~~**`record-sync` 只能登记 HEAD，补登不了历史 sync merge**~~ | TASK-104 合并实测 | **已闭合 2026-08-23** | 按当时写下的修法做了：`record-sync` 加 `--hash`，复用 `record_commit(--hash)` 同一条 rev 解析，**形状校验一条不放松**（补登对象照样要是「有后位亲在 main 上」的 merge）。守卫两条，含反向那条；变异验证：拆掉形状校验 → 反向那条转红 |
| 3.6.7 | **merge 记录的形状不统一，`cleanup` 因此认不出已合并的 Change** | TASK-102 清单实测 | 中 | 手工合并时人写的是 `merge_commit` / `at`，而 `cleanup` 读的是 `merge["hash"]` → 对一条**确实已合并**的 Change 报 `BLOCKED_NOT_MERGED`。本次按加法补齐了 `hash` / `time`（未动既有字段）。根治要么给 merge 记录一个单一来源的写入口，要么让读侧接受两种键名 —— 前者对 |
| 3.6.8 | **`writeback_needed` 恒为 `True`** | TASK-101 补审轮 2 non-blocking（判 P3） | 低 | 回写已提交后重跑 `record-commit` 仍答「有待办」，并给出会 `nothing to commit` 的命令。主路径本来就是无条件给的，非新引入；真实脏否由 `_dirty_gate` 在下次操作时判定 |
| ~~3.6.9~~ | ~~**`git branch -d` 在「本地领先其远端上游」时拒删，即便两个 tip 都已在 main 内**~~ | `feat/wfm1-batch-c` 清理实测 | **已闭合 2026-08-23** | `cleanup` 现在**自己回答那个问题**：分支 tip 是 `origin/main` 的祖先 ⇒ 全部历史都在 main 里 ⇒ 删除不可能丢东西，此时才升级到 `-D`；答不出来仍然拒绝，并把理由写进返回值。**这不是放宽**，是把判断从 git 的那个近似（相对本地上游）换成真正要成立的那条。`feat/wfm1-batch-c` 本地与远端**都已删除**，欠了一整轮的清理到此结清 |

**一条方法论**（与 §7 同族）：这四条里有三条的形状是**「事实成立，但登记表说不出来」** ——
不是守卫太严，是**事实的表示法有两套而读侧只认一套**。3.6.3「L0–S7 合同手抄三份」
是同一个病在文档侧的样子。

## 4. 数据与状态的诚实性（每条都是「界面说的和事实不符」那一族）

| # | 欠账 | 来源 | 风险 | 说明 |
| --- | --- | --- | --- | --- |
| 4.1 | **`storageState` 写入 `missing`** | TASK-077 §6.5 | **高**（持久化） | 探针只显示、不改写登记。把声明与磁盘对齐是持久化改动 |
| 4.12 | **`approveShot` 双写：层 1 Decision 与旧标记各携带对方没有的东西** | TASK-074 §1.3 边界判定（2026-08-25） | **高**（持久化 + 闸门 + 已知会不一致） | 「删除旧布尔标记，只留层 1 Decision」今天做不到：Decision 有 `basedOnVersion`（必填）但**没有** `assetId` 与 `note`，而 `isApprovedFor` 判的正是 assetId（身份，比版本序数强），`note` 是创作者写的字（§1.3 硬规则：不删用户数据）。**还要定一件事**：一条旧的、没有版本号的通过记录在 Decision 的词汇里表示不出来 —— 代码里已写着这一点（`app.js` 撤销通过那段）。三条出路与各自代价列在 [TASK-074 §1.3](TASK-074-delivery-migration-and-legacy-retirement.md) 的「划边界」小节。**今天就存在的真实后果**：撤销通过那条路上两份记录会不一致（旧标记说没通过，最新 Decision 还停在 `passed`），代码选择「撤销照样生效」并写进了注释。要做的是一次 ReviewDecision 合同变更（加 `basedOnAssetId` + `note` + 未版本化通过的表示法）→ **需要 ADR**（AGENTS.md 第 21 条） |
| 4.2 | ~~**后端媒体探针路由** `GET /api/projects/<p>/media-audit`~~ **已闭合（TASK-103 批次 C · `32b33bc`）** | GAP-02 · TASK-083 §5.2 | 中 | 服务端直接看文件系统 → **没有跨源问题，没有 `INCONCLUSIVE`**。很小 |
| 4.3 | ~~**资产登记表没有像素尺寸、视频无实际时长**~~ **已按「只读探测」那条路闭合（TASK-103 批次 C · `32b33bc`）** | TASK-079 §7 | 高（schema）或 中（只读探测） | 审片页那两列因此写「未记录」。两条路：登记时探测并存储（schema + 迁移），或照 `mediaprobe.js` 的先例做只读探测 |
| ~~4.4~~ | ~~**`nonEmpty` 拦不住占位词**~~ **已闭合（2026-08-24）** | TASK-078 | 中 | 两个编译器（`skillpkg.py` `_PLACEHOLDER_WORDS` / `skills.js` `PLACEHOLDER_WORDS`）各加一份**逐字相同**的占位词名单，`nonEmpty` 为真时整串命中即拒。**判据是 trim + 小写后整串相等，绝不是子串包含** —— 「无人机俯拍」「常规打光之外的处理」都是真答案，子串匹配会把它们一起拒掉，那比放进无信息的内容**更糟**（前者拒真内容，后者只是接受空话）。空串仍报「不能为空」而不是「占位词」：没生成出来与生成了但没内容，把创作者送到不同的地方。没要求 `nonEmpty` 的字段不受影响 —— 可选字段填「无」可能正是本意。测试：JS 4 条 + 跨语言合同 5 条（含两份名单逐字比对与扫描面非空自检） |
| ~~4.5~~ | ~~**`LANDS_ON` 与 `MODULE_ALIAS` 两份表无守卫比对**~~ **已闭合（2026-08-24，按登记里那条更小的路）** | TASK-086 §Follow-up | 低 | 新增 `tests/contract/test_module_alias_mirror_task087.py`：镜像表写下的每一项都要和 `shell.js` 的权威表一致，写了权威表没有的键也红。**没做根治**（让工具直接从 `shell.js` 读，要给纯 Python 工具加 JS 解析）——登记里点名的危害是「两份表本身不比对」，这条测试正对着它。值里的常量会被解引用（`storage: [PROJECT_SETTINGS, …]`），不硬编码 `projectsettings`：硬编码的话常量一改这条测试跟着错，而它的全部职责就是「别让两处各写各的」。带扫描面非空自检 |
| 4.6 | 升版本后**历史 Run 的 Prompt 无法从磁盘复现** | TASK-078 · TASK-094 批次 A/C/E | 中 | Run 记了 `skillDigest`，但那个版本的 `prompt.md` 已被新版本取代。**本链又升了三个包**（`episode-planner` 2、`story-development` 2、`script-breakdown` 2），所以这条的暴露面从一个包变成四个。**不是运行期缺陷**：没有任何代码按 `(skillId, version)` 解析包（`findSkill` 只按 id），历史 Run 的产物早已存在文档里，所以不会 fail closed；丢的是**可复现性**。根治要给包一个带版本的磁盘布局（`<skillId>/v1/`…），那是 ADR-0067 的布局变更，需要单独一张卡 + ADR。codex 在批次 E 把它报成 blocking，已按此驳回并登记（ADR-0067 决策 3 给出的补救就是「升版本号」，它没有要求保留旧内容） |
| ~~4.8~~ | ~~**Prompt 漂移基线只覆盖 20 / 22 个内置包**~~ | TASK-094 批次 A 实测 | **已闭合（2026-08-23 复查发现早已修好）** | `skill-prompt-snapshots.json` 现在 26 条，与 `product-skills/builtin/` 的 26 个目录一一对应；`test_the_two_compilers_agree_on_every_skill` 的成员集合断言写的是 `set(expected) == set(catalog.skills)` —— **从目录派生**，新包因为存在而进基线，正是本条要求的那个形状。本条属于「文档比事实旧」那一族，登记时是真的，读到时已不是 |
| 4.9 | 拆解提案的 `existingAssetKey` **只显示、不绑定** | TASK-094 批次 E | 中 | 能力现在能说「这个人已经有参考图了」，卡片会**对着资产库核一遍**再显示（核不到就说核不到）。但「确认后把这张图挂到该档案上」还是手工的。自动绑定＝资产登记写路径，超出 TASK-090 §2.2 的范围（那一节只要求输入 + prompt + 端点），故未做 |
| ~~4.7~~ | ~~规划版本的 `basedOn` **只校验「是整数」**~~ **已闭合（2026-08-24）** | TASK-094 批次 A · codex 复审非阻塞 | 中（持久化诚实性） | 新增 `identity.js` 的 `basedOnOrNull(value, v)`：父版本必须是**真实的更早版本**（`1 <= basedOn < v`）。三处一起收紧（`scriptdoc.js` 一处、`storydoc.js` 两处）—— 登记里就点名了「大纲链有同样的宽松，一起收紧才有意义」。**越界回 `null`，不夹到 `v-1`**：`null` 是这三处早就写好的诚实默认值，而夹成 `v-1` 恰恰是那几行注释禁的事（凭空发明一条溯源链）。住在 `identity.js` 是因为它本来就owns「身份只携带、绝不从序号再推导」这条规矩。测试 3 条，含一份被篡改存档的水合对照 |
| ~~4.10~~ | ~~**「每次粗剪 / 每次导出 → 新版本，旧版本仍在」没有任何测试**~~ **已闭合（2026-08-24）** | TASK-074 §1.4 归属清点 | 中 | **两层一起**补上。① 真实写路径 `tests/studio/test_final_cut_versioning_task074.py`：服务端 `_agent_compose` 用 O_CREAT|O_EXCL **原子占号**，在**磁盘上**验旧版本还在且内容未被改写、已有版本之间的空位不靠覆盖填、并发占同一个号必失败；另有源码守卫钉住生产代码仍在用 O_EXCL（改成「先查再写」即红 —— 那道竞态就是覆盖的入口）。② 闸本身 `fullpipeline.test.mjs`：连开五版旧版本一个不少；覆盖任何已存在的版本被拒；倒退被拒；非整数被拒（否则 `undefined` 会绕过整条判断）。它守的正是 AGENTS.md 第 13 条「禁止静默覆盖已有生成结果」，也让「代码里不存在覆盖分支」这句话从声明变成可检验的断言。变异验证：两道守卫一起去掉即红（单独去掉一道时**第二道接住了** —— 纵深防御，不是测试漏了） |
| ~~4.11~~ | ~~**成片版本号：服务端「填坑」，前端 G5「只前进」—— 两条规则相反**~~ **已修（2026-08-24，当天登记当天关闭）** | TASK-074 §1.4 边界 8 实测 | 中（产物身份） | 服务端 `_agent_compose` 原本从 1 往上找**第一个空位**：创作者删掉中间某一版之后，新成片会落回一个**低于当前最高版本**的号，而按「最高版本 = 当前成片」认产物的下游会把**刚做出来的那一版**当成旧结果。已改为从 `max + 1` 起算，与 `gates.js` 的 `g5Append`（「版本只前进」）一致；`O_EXCL` 保留 —— 它挡的是并发算出同一个号。**登记时我写的是「往哪边统一需要一次判断」，那是错的**：codex 复审指出这不是两种都成立的设计，是一个有明确后果的缺陷。把缺陷钉成期望值是最不该做的一种「如实报告」 |

## 5. IA 与交互的残留

| # | 欠账 | 来源 | 风险 | 说明 |
| --- | --- | --- | --- | --- |
| 5.1 | **`workbench` / `provenance` 的内容搬迁** | TASK-073 §5.11 · TASK-074 · TASK-086 | 中 | TASK-086 只让**地址**说了真话（解析到自己）。内容仍没搬，两页仍在 IA 十一页之外，rail 不高亮。搬走之前它们的存在本身是诚实的 |
| 5.2 | ~~`ui.genIntent` 只按媒体种类存一份~~ **已闭合（TASK-103 批次 D · `f05729d`）** —— 改按 `(kind, shotId)` 存，抽成 `ui/genintent.js` | TASK-078 | 中 |
| 5.3 | Skill 的「上次跑出什么」内容视图 | TASK-080 §5 | 低 | 会话里有按能力过滤的运行记录，但没有产出内容视图 |
| 5.4 | 会话里的自由文字与非镜头 `@` 引用**不进 Prompt** | TASK-080 §5 | 中 · ADR-0067 范围 | Skill 输入是声明式的（ADR-0056 决策 6）。界面已逐项明说，不是静默丢弃 |
| 5.5 | 三步向导仍走演示预算 | TASK-078 | 中 |
| 5.6 | ~~`honourAddress` 等项目加载期间丢 popstate~~ **已闭合（TASK-103 批次 D · `f05729d` + 轮 1 P1 修复）** —— 闩改「合并」并携带事件发生时的地址 | TASK-081 §Follow-up | P3 | 快速连按前进/后退可能被先到的路由覆盖。地址最终会被 `writeUrl` 规范成实际所在处，**屏幕与地址不会不一致** |
| 5.8 | ~~分集规划里**未确认**的规划条目不可逐格编辑~~ **已闭合（TASK-103 批次 D）** —— 寻址扩成「有 id 用 id，没有就用 `ep#<本版第 N 集>`」，仍是身份不是索引 | TASK-094 批次 B | 中 | 每个规划编辑都按 `episodeId` 寻址，而未确认的条目还没有实体（`episodeId: null`），所以那一行**如实说明「确认后才能改」**而不是给一个会被拒的控件。要让它可编辑，需要把寻址扩成「有 id 用 id，没有就用 epNumber」——那是域层寻址合同的改动，本链没做（AGENTS.md 第 17 条）。真实项目的 v4 已确认，主流程不受影响 |
| 5.9 | **界面上没有「提出审片问题」这个动作** | TASK-103 批次 B · codex 轮 1 | 中 | `workflow/review.js` 的 `issue()` 有完整实现与 schema，但在整个 `src/` 里**零调用方**。于是 `create-feedback` 这条已注册的 Gateway 命令无处可接，AI 导演说出的问题也留不下来。要接通得先有「提出问题」这个动作（谁提、落在哪一层、和 Agent 提案的关系），那是一张独立的卡 |
| 5.10 | `create-action` / `action-transition` **已注册但无调用方** | TASK-103 批次 B | 低 | 与 5.9 同源：行动闭环的后半段（把一条反馈变成一个行动、再推进它的状态）在界面上没有落点。注册它们本身是对的（TASK-083 §5.1 要求的就是注册表接线），但要如实说它们今天只有 API 面 |
| 5.11 | 核心登记中断后**只有手动重试** | TASK-103 批次 B · codex 轮 1 P1 修复 | 低 | 请求飞在半空时关掉页面，台账留下 `interrupted` 并在审片卡片上给出「重试登记」按钮 —— 这条是**诚实**的，但不是**自动**的。自动重试要定重试策略、去重与退避，超出本卡 |
| 5.12 | `media-audit?measure` 的 **ffprobe TOCTOU 只能检测，不能阻止** | TASK-103 批次 C · codex 轮 3（由 non-blocking 升为 blocking） | 低（已缓解） | `ffprobe` 接的是**路径**不是描述符，而 `/dev/fd/N` 只有 POSIX 有，Windows 是权威环境（ADR-0062）—— 所以校验与读取在这里做不到原子。已做的是**检测**：探测前后各取一次身份（resolve 后的路径 + inode/dev/size/mtime），对不上就丢弃结果，于是**读**仍可能发生但**泄露**不会。残余风险要求攻击者已经拥有项目 media 目录的写权限 —— 有该权限者可以直接放真文件进去，或替换应用本来就在渲染的任意媒体。轮 5 又报了一个 **ABA 变种**（换走 → 让 ffprobe 读 → 换回来，前后身份因此相等）—— 同一机序的第三次报告，且**换一层检测就会有下一个变种**。按 ADR-0081 §2b/§2c（按失效机理判同题、不追更窄的拼法）停在这里并记录。根治只有一条：把探测做进进程内（自己解容器头，不再把路径交给外部进程），那是另一张卡 |
| 5.7 | **Phase 4 视觉统一**（空态 / 密度 / 层级） | ui-correction-plan Phase 4 | 低 | 计划里的失败态工单化与项目卡进度**已在 079/082 做掉**，Phase 4 实际只剩这一条。建议等 5.1 搬完内容再做，否则改完还要再改 |
| 5.13 | **界面把创作者指向「生成记录」，而那一页没有入口** | TASK-074 §1.5 批次 3 · §1.6 复查（2026-08-25） | **中～高**（不是缺功能，是**界面说了不算数**） | `ui/genrecord.js` 在 `src/` 里**零 importer**，而它是**唯一**渲染 Skill ID / Skill 版本 / Runtime / Executor / Model 的地方。IA §6.3 把这些技术词汇从主界面「删除」，原话是「moved here」不是 erased；`ui/agentpanel.js:199` 更是直接写着「在每个结果旁的『生成记录』里」—— **一句界面上的承诺，而那个地方今天到不了**。已挂载的 `director.js` / `prodinspector.js` 里也有叫「生成记录」的分区，但它们渲染的是来源与链路，**不含上面那五个字段**。所以这不是「重复实现，删一个」，是**两份东西同名而覆盖面不同**：要先判哪一份是权威，再接入口。**删掉 `genrecord.js` 会真的删掉能力**（§1.5 规则 2），所以 §1.5 保留了它 |
| 5.15 | `workflow/artifactversion.js`：**系统合同 §3 的六态词汇零消费者** | TASK-074 §1.5 批次 3 · import 图实测 | 低 | 只读、零写入，有自己的测试与一条 `tests/contract` 守卫，但 `src/` 里没有任何模块用它。与 5.13 同族（有实现、有测试、没有入口），但**没有任何界面文案声称它已接通**，所以只是没用上，不是说话不算数 |
| 5.14 | `ui/timelinews.js` 的**死界面**与仍在用的读模型混在一个文件里 | TASK-074 §1.5 批次 3 | 低 | `production.js` 有具名注记：ADR-0061 决策 6 用后期制作台取代了剪辑工作区，模块留在树里但没有任何东西渲染它。`renderTimelineWs` / `bindTimelineWs` 是死的；`timelineModel` 仍被 `av.test.mjs` 用。清理它要先回答「读模型该住哪」，那是位置问题不是清理问题，故 §1.5 保留 |

## 5.16～5.20 TASK-121/122/123/124 收口时交来的五条（2026-09-04 登记）

四张卡的**主体已交付**，剩余事项登记在这里，卡随即进 `done/`
（[收敛审查](../../design/active/product-requirement-and-ux-convergence-review.md) §8.3）。

| # | 欠账 | 来源 | 风险 | 说明 |
| --- | --- | --- | --- | --- |
| 5.16 | **提案区最多占右栏 42%，条数多要自己滚** | TASK-121 Follow-up | 中（对话空间被挤） | 更好的做法是**只钉最近一条未答复**，其余以一行「还有 N 条」折叠进对话历史；输入框只占内容所需高度，消息流给一个可用高度下限。收敛审查 §4 与 §6 高优 1 是同一条 |
| 5.17 | **开发派工通道没实现** | TASK-122「还没做的」 | 中 | 形状已写进 [ADR-0093](../../adr/ADR-0093-approved-plans-build-themselves.md)（他在提案卡上点「同意」→ 起一轮有工具的 `claude-code-dev` → 改代码、跑测试、提交 → 结果写回卡片），**代码一行没动**。ADR 已 Accept，缺的是实施 |
| 5.22 | **录白膜录到的是旧画布** | codex 块 2b 补审（2026-09-05） | 中 | `bkRecord` 捕获画布之后调用 `render()`，而 `render()` 会**替换**那个 canvas 节点，随后 `bkPlay` 把动画画在**新**节点上 —— 录制流仍然连着已经被换掉的旧画布，于是录出来的白膜不含播放动画。来自 `23f80ec`（TASK-123），不是补审那批改动带进来的，按第 17 条登记不顺手修。与 §5.18 是同一处功能的两件事：那条说的是**没在真浏览器里验过**，这条说的是**验之前就已经知道错在哪** —— 修法方向是先 `render()` 再捕获，或让 `render()` 复用同一个 canvas 节点 |
| 5.18 | **录白膜没在真浏览器里跑通过** | TASK-123「还没做」 | 中 | `MediaRecorder` 需要真实显卡合成路径，headless + swiftshader 下验不了格式/权限/停止/刷新恢复/版本化写入。**在验过之前界面继续标为实验能力** —— 这条是诚实的，不是缺陷 |
| 5.19 | **制作画布镜头卡缩略图多数是「还没有画面」** | TASK-124「还没做」 | 低～中 | `poster` 取的是镜头记录里的字段，**关键帧已经做出来的镜头应当显示它**；缺媒体时显示原因，不用通用图标冒充内容 |
| 5.20 | 画布卡片的拖拽排序 / 按场景分组 / 批量选中 | TASK-124「还没做」 | 低 | 等真实使用之后再说，**不预先做** |
| 5.21 | **持久化的交付层审片问题不进 G4** | TASK-130 造样本时发现（2026-09-05） | 中～高（**界面说了不算数**那一族） | `g4Export(report)` 只读 `runDeliveryQc` 从**探测**算出来的报告；创作者 / Agent 经 `review.issue()` 记进 `reviews.issues` 的 `layer: "delivery"` 问题，哪怕 `severity: "blocking"`、`state: "open"`，**导出闸门看不见**。今天界面上还没有「提出审片问题」的动作（§5.9），所以没人撞到 —— 但 §5.9 一接通，第一条人工标记的阻断问题就会被导出绕过。修法方向：`exportability()` 的报告绑定之后再并入 `reviews.issues` 里 `layer === "delivery" && state === "open" && severity === "blocking"` 的那些（它们本来就是 §6.5 说的 ReviewIssue）。TASK-130 的旅程因此用**真实测量的规格不符**触发阻断，不用种问题冒充 |
| 5.22 | **`gate_dispatch.py` 用 `Path.cwd()` 解析仓库根，不是脚本位置** | TASK-131 切片 A 实施时发现（2026-09-05） | 中（**闸门可能去错地方找自己**） | `.claude/hooks/gate_dispatch.py` 里 `root = Path.cwd()`，随后拼 `root/.claude/hooks/gate.ps1`。客户端若以非仓库根为 cwd 触发 PreToolUse，它会去一个不存在的路径找 gate 脚本。今天没观察到实际失败（Claude Code 目前以仓库根为 cwd），所以这是**潜在**缺陷而不是活缺陷 —— 但它与 TASK-131 给新工具定的「根由脚本位置解析、可从任意子目录调用」是同一条纪律，而闸门比诊断工具更不该赌这一点。修法：`Path(__file__).resolve().parents[2]`，并在 `tests/tooling/` 加一条从子目录调用的用例。**不在 TASK-131 范围内顺手修**（AGENTS §17） |
| 5.23 | **`SessionStart` 接线没落地** | TASK-131 切片 C（2026-09-05） | 低（是增强，不是缺陷） | `agent_harness.py resume --brief` 已经写好并测好（干净的树 + 没有过期快照时输出空串），但把它挂进 `.claude/settings.json` 的 `hooks.SessionStart` 这一步被**实施 Agent 的会话权限层**拒绝 —— 配置文件受保护，这是外部限制，没有绕过。要加的那段 JSON 原文在 [handoff.md](../../../.claude/skills/dev-workflow/references/handoff.md) §3。**不接也不影响任何东西**：同一条命令自己跑一遍结果一样，缺 hook 支持不得变成一道新的人工闸（AGENTS §1） |

## 6. 性能与偶发（记录，不承诺）

| # | 项 | 来源 | 说明 |
| --- | --- | --- | --- |
| 6.1 | 每次 Bash/PowerShell 工具调用多 **~126 ms** | TASK-085 §6 | policy 冷启动。压它的正确方向是常驻/内联切分，**不是**把文本预筛加回来（那会重新引入 TASK-085 消除的漏洞） |
| 6.2 | `test_the_kill_verified_finish_PERSISTS…` **偶发失败** | TASK-085 §6 | `os.replace` 收到 `OSError`；单独重跑与整个串行阶段重跑都通过。形状像 NTFS 瞬时共享冲突（杀软/索引器）。**根因未查**，但它的一个后果已在 TASK-056 闭合：`_persist_locked` 失败时不再留下 `runs.json.tmp<pid>` 残骸（实证：仓库里躺着 219 KB + 27 B 两个，各是注册表的整份拷贝）|
| 6.3 | 分类器彻底不可达时会拦**每一条**命令 | TASK-085 §6 | 实际接线里不可达（`gate_dispatch.py` 自己就是 python），影响面已压到最小 |
| 6.4 | **`data/skills/` 仍在仓库里** | TASK-056 | `_USER_SKILLS_DIR = DATA_DIR / "skills"`。按它自己的注释（合同 §5.5）它和 `projects.json` / `runs.json` 同类 —— 账户级、跨项目、非源码 —— 但 TASK-056 的范围只写了那两个注册表，所以它没跟着搬。**不是缺陷，是一致性欠账**：ADR-0067 的用户 Skill 包目前仍从仓库 scratch 读 |
| 6.5 | `pytest_unconfigure` 不还原 `MOTV_APP_DATA_DIR` | TASK-056 审查轮 4（P3） | 用 `pytest.main()` 在同一进程里跑完测试的调用方，之后仍持有一个**已被删除**的丢弃目录路径；再起后端会把它重建出来并往里写。命令行 `pytest` 不受影响（进程结束变量就没了）。**未修** |
| 6.6 | `RunStore._load` 先 `exists()` 再读 | TASK-056 审查轮 4（P3，审查者自标 uncertain） | 两步之间若有另一进程创建了新位置的日志，本实例会从 legacy 启动并可能用旧状态覆盖新的。同一台机器上同时跑两个后端本来就不是支持的姿态（单用户 loopback 原型），且窗口是微秒级。**未修，记录** |
| 6.7 | `cleanup` 删分支前的两项检查存在残余竞态 | TASK-087 §3.6.9 实施 · codex 审查轮 3 | **记录，不修**。删除已经用 `update-ref -d <ref> <expected-oid>` 把**分支自己的 oid** 钉住了（轮 1 的 P1）。另外两项钉不住 —— git 没有跨这些条件的 compare-and-swap：① `origin/main` 在检查与删除之间被**改写**（前进不影响包含关系，只有 force-push main 才会；而那在本仓库是禁的）；② 另一进程恰在这几微秒里把该分支 check out 到新 worktree。按 ADR-0081 §2b 判为轮 1 那条 P1 的**更窄变体**（失效机理同一个：删除前的检查可能已过时），做范围判断并记录，不再逐个追拼法。代码里就地写明，见 `cleanup` 删除前那段注释 |
| 6.8 | `workspace_shell` 的空 Portfolio **不说明自己在找什么** | ADR-0086 决策 3 | 账户根下一个核心项目都没发现时，它渲染一个没有解释的空列表。按 ADR-0086 这**不是缺陷**（那些项目本来就不归它管），但它应当说出自己找的是 `config/wfm1.json` —— 「装了却没生效必须可见」在这里同样适用。**未做，不阻塞** |
| 6.9 | 创建项目时 `flow_id.strip()` 与加载器的 id 规则不一致 | TASK-105 审查轮 3（P3） | 加载器只要求 `flowId == 目录名`，而目录名在 POSIX 上**可以带首尾空白**；创建路径先 `strip()` 再查，于是这种边缘包会「列得出来但选不了」（404）。根治是让 manifest 直接拒绝首尾带空白的 `flowId`，那样 `strip()` 变成空操作。**未修**：目前没有任何真实包是这种形状，且两侧都不会造出错误的项目 —— 最坏是选不了并报 404 |

---

| 6.10 | 画布被判无效时，应用**封锁保存却不说** | TASK-111 真机验证时撞到 | 文档不通过 `migrateToCurrent` 时，前端加载一张空白画布并把这个项目的保存置为 `_blocked`（fail-closed，是对的）。但界面照常可编辑、可发送 —— 实测打字、对话都正常，只是没有任何保存请求发出去。**缺的是说出来**：屏幕上要有一句「这个项目的画布读不出来，改动不会被保存」。**未修，不阻塞** |
| 6.9 | **多会话同时跑全量时，闸门会在 600s 处超时** | 2026-09-05 实测（TASK-131 收口时） | 实测：跑到 **99%、零失败**，然后被 `gate.ps1` 的单步超时砍掉，报成 `pytest (full, parallel) failed`。当时同一棵工作树上有 **8 个会话**在跑，其中若干在跑各自的全量。**这不是改动的问题，是机器的问题** —— 下一个撞上的人如果不知道，会去 debug 一个不存在的缺陷，而全量一次 6–7 分钟，debug 它的代价很高。识别方法：看输出末尾是 `[timed out after 600s]` 还是 `FAILED`；前者且进度接近 100%、零 `F`，就是它。**不改超时值** —— 把上限调高是把「机器过载」伪装成「跑得慢一点」，而超时本身是对的（一个永远跑不完的闸门等于没有闸门）。真正的对策是降并发，或给每个会话独立的 `git worktree`（各自的 index 与工作树，同一个仓库，最后各自推分支）—— 后者同时消掉本条与「写到一半的中间状态锁住所有人」那一族 |

## 7. 一条贯穿本批的方法论（值得单独读）

TASK-077～086 里真正的缺陷有**六条是同一个形状**，而且没有一条是「实现写错了」：

| | 形状 |
| --- | --- |
| `_package_dirs` | 屏蔽表键在 `skill_id=""` 上对不上，注释把危害写得很准，代码从未生效 |
| `gate.sh` 分类器 | 预算注释写着 must never fail open，而它是唯一没超时的一步 |
| `is_symlink()` | Windows junction 返回 `False`，守卫恰好抓不到它要防的那个东西 |
| 媒体探针 | 「问不出来」被记成「文件不见了」，永久 |
| 往返测试键集 | 性质是对的，**覆盖的集合恰好排除了坏掉的两个成员** |
| 短选项簇 | 簇内不再过度匹配了，**调用点还在做成员测试** |

共同点：**不变量只覆盖了一半，另一半就在相邻那一层。**

推论，已在多张卡里生效，建议继续执行：

1. **断言性质，不要断言写法**（TASK-077 跨源、TASK-086 往返集合）。
2. **守卫的键集要派生，不要手写**——新成员应当**因为存在**而进测试。
3. **每条新守卫做变异验证**：改坏实现看它是否变红。但要记住变异验证抓
   「实现被改坏」，**抓不住「用例本身没覆盖到的形状」**——后者只有独立审查买得到
   （TASK-085 三轮买到的正是它）。
4. **杀不掉的变异如实记录**，不要编一个断言非法输入的测试凑绿点
   （TASK-085 短集合里的 `U`）。
