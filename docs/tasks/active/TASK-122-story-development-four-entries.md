# TASK-122：Story Development 按他的规格重构 —— 四个入口

- 状态：进行中（2026-08-30，第 1–2 步已落地）
- Workflow：Feature · 深度：DEEP
- 关联 Requirement：[REQ-006](../../requirements/REQ-006-agent-can-do-what-the-creator-can-do.md)
  判据 1（Agent 能做创作者能做的事）与判据 7（两个窗口）；本卡的产品规格由产品负责人
  2026-08-30 在应用里直接下发，并以提案 #4「要改」+ 规格全文的形式记在台账里
- 关联 ADR：[ADR-0092](../../adr/ADR-0092-story-development-is-four-entries.md)
- 起因：他在应用里给出的重构规格 —— 左栏严格四个入口、大纲要有稳定 Outline Node ID、
  结构规划是固定九列表并能引用大纲节点、正文创作先选小说/剧集且支持三条路径、
  章/集不进左栏、日常只留最新版而定稿才存历史、不新增一级页面或 Tab

## 六步（他要求实施前先出方案，方案已作为提案 #7 发给他）

| 步 | 内容 | 状态 |
| --- | --- | --- |
| 1 | 数据模型（形态 / 大纲节点 / 结构规划 / 章集正文 / 定稿历史） | ✅ 完成 |
| 2 | 左栏换成四个入口，分集移出左栏进页内选集器 | ✅ 完成 |
| 3 | ① 故事核心 + ② 故事大纲（节点化编辑） | ⛔ 未做 |
| 4 | ③ 结构规划表 + 引用大纲节点 | ⛔ 未做 |
| 5 | ④ 正文创作（模式、Planned 数量、章/集选择器、二级 Brief、编辑器、Copy） | ⛔ 未做 |
| 6 | 定稿/历史规则接进界面 + Agent 动作 + 逐条验收 | ⛔ 未做 |

## 第 1 步：数据模型（`src/workflow/storywork.js`）

住在 `story.work` 里，与既有的创意简报 / 大纲版本 / 分集规划**并存**，一条不删。

- `form`：小说 / 剧集，**不替他默认**
- 大纲节点：作者写文本，系统切节点并发**稳定 id** —— 改错别字、中间插段落，
  其余节点 id 一个不变（结构规划的引用因此不会集体断掉）
- 结构规划：他点名的九列，最后一列引用节点；引用失效**会被指出来**；删行是软删除可撤销
- `planned.novel / planned.episode`：**可增可减**，减少时既有章/集一个不删
- 定稿：日常编辑不产生历史；`finalizeUnit` 才存一版，内容没变不重复存；
  历史可看、可恢复、**可手动删**，删历史不动当前正文
- `seedPlanFromEpisodes`：现有 12 集可一键变成结构规划的行，且只灌一次

## 第 2 步：四个入口（ADR-0092）

左栏画出来的严格四行：故事核心（brief）/ 故事大纲（story）/ 结构规划（episodes）/
正文创作（script）。`settings` 收起（页面与地址都在，只是不画）；分集行进页内选集器
（`epPicker`，默认只显示当前那一集，点开才列全部），数据与旧左栏**同一份来源**。

**这一步改了被 ADR 冻结的 IA**，所以先立了 ADR-0092，再按它更新那些守卫测试 ——
每一条新断言都指回 ADR，并保留旧规则的出处。被更新的：`bibleposture`（作品设定的位置）、
`creatornav`（分集行的行为，搬到选集器上照旧成立）、`wizardskeleton` / `workspaces` /
`upstream`（script 的归属、EPISODE_NAV 的第一行、生产链）。
`crumbScope("script")` 补了一条显式规则：正文仍是**集级**，不能因为换 space 就退成项目级。

## 验证

- 前端全量 **1994 passed / 0 failed**（新增 `storywork.test.mjs` 17 条）
- `PAGES.length === 11` 冻结守卫照旧成立；`resolveModule('characters')` 仍解析到
  `settings#characters`（没有任何东西变得够不着）

## 待办（下一步）

第 3 步开始动界面内容：故事核心的大编辑器、故事大纲的节点化编辑。
