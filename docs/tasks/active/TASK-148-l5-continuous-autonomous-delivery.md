# TASK-148：L5 —— 给一个方向就连续交付，而且没人看着的时候不会静默放行

- 起因：产品负责人 2026-09-16 —— 「我想让这个项目能实现 level5 的开发模式。」
  L4 / L5 的定义出自 `.claude/tmp/l4-claude-handoff-20260915.md`（一次性产物，
  结论提炼进本卡后删原件 —— AGENTS.md §26）：
  **L5 = 限定范围内持续自主交付** —— 在用户已确定的方向与候选任务中**自动取下一项**、
  跑完与 L4 相同的闭环，遇外部阻塞保留状态并报告；产品方向、审美判断与新增付费仍归用户。
  · 闸：**放行（第 ② 问：阻塞在办主线）** —— 依据是下面 §1 的两条实测，不是「这个想法好」。
- 类型：Refactor（工装，不改产品行为）· 深度：**DEEP**
  （C=DEEP：把**权威** enforcement 从 Claude Code 的 `PreToolUse` 迁到 git 原生 hook，
  是对 [ADR-0050](../../adr/ADR-0050-powershell-native-agent-dev-tooling.md) 决策 1 那条
  实现约定的再一次取代；I=DEEP：波及每个会话的每一次提交。U/R 均为 STANDARD/QUICK，
  取最高档）
- 成果物：ADR-0104（enforcement 权威迁移）· 本卡 · 提交。**无产品 REQ** —— 工装按
  AGENTS.md §20 写技术目标。
- 技术目标：**让「给一个方向 → 连续把任务做完并落地」不需要逐条催促，
  并且在没人看着的时候不会静默放行。**
- 架构约束：`CA §1`（`.claude/` 是 agent 工装，不是产品代码）· `CA §4`（工装测试归
  `tests/tooling/`）· AGENTS.md §14/§16/§17（单一实施者、动手前查归属、不顺手修范围外）·
  §20 fail-closed · §22 merge 前置条件一条不减
- 实施 Agent：`visual-try-on-project-b0`（2026-09-16 认领）
- 工作树：`D:/02_Work/04_video-work/motv-wt/TASK-148-l5`，分支 `change/TASK-148-l5`，
  由 TASK-147 交付的 `worktree.py new` 建出（**本卡跑在 L4 刚交付的那条路径上**）。

## 1. 为什么 L5 的第一件事不是「自动取下一项」

L5 要的是**无人催促地连续跑**。今天的闭环里有两条已经实测的破口，它们在 L4 下只是
恶心，在 L5 下变成**静默放行错误代码**：

| # | 实测 | 出处 |
| --- | --- | --- |
| 1 | **闸门会静默不触发** —— 14 次提交尝试中 5 次 `PreToolUse` 未被调用，且**不可由命令形状复现**。结论：`PreToolUse` 可作 early feedback，**不能当 authoritative enforcement** | [TASK-147](TASK-147-session-isolation-and-evidence-identity.md) §6.2 · §8 |
| 2 | **旁树完全没有闸门** —— 在 `motv-wt/pilot-143` 里暂存一个 ruff 必报三项（I001 + F401 + 格式）的文件，`cd <worktree> && git commit` **直接成功，闸门零输出**。机理：钩子按会话的 `CLAUDE_PROJECT_DIR` 定根，`cd` 换得掉 git 的树、换不掉钩子的树 | [TASK-143](../backlog/TASK-143-one-worktree-per-session.md) §「第 8 种形状」（2026-09-16 实测） |

两条合起来是同一句话：**闸门的沉默，既可能是「检查全过了」，也可能是「它根本没跑」，
从输出上分不出来。** L4 时人在每一步旁边，漏一次有人看见；L5 没有那个人。

**而且第 2 条正好打在 L4 的交付物上** —— L4 把每个会话搬进各自的 worktree，
恰恰是让闸门失效的那个动作。TASK-147 决定 A-1 断言「hook 与配置装一次覆盖所有树」，
那句**对 git 原生 hook 成立，对 Claude Code 的 `PreToolUse` 不成立**。
这不是 A-1 判错，是 A-1 说的那个「hook」和闸门实际所在的那个「hook」不是一回事。

所以切片顺序是定死的：**没有 A，B 和 C 是灾难放大器。**

## 2. 三个切片

**切片 A：闸门成为权威（先做）。**
git 原生 `pre-commit` 成为 authoritative enforcement；`PreToolUse` **不删**，
降级为 early feedback（快反馈本身有价值，只是不再是唯一防线）。
实测支撑已在 TASK-147 A-1 拿到：worktree 的 `git rev-parse --git-path hooks` 解析到
**主 `.git/hooks`**，`git config` 也跨 worktree 共享 —— **装一次覆盖所有树**，
第 1 节第 2 条因此被闭合，而不是被绕开。需要 ADR-0104。

**切片 B：度量台账。**
任务书 §6 的五项：工程介入次数 · 自主完成率 · 交付耗时分项（实现/测试/审查/等待/返工）·
质量（错误合并、串入他人改动、失效证据被接受、交付后回退）· 用量（取不到记未知，
**不把估算当账单**）。append-only，**不造第二份状态文件**（TASK-143 OUT OF SCOPE 已写死）。
没有它，「L5 达标了吗」只能靠感觉答，出事也没法复盘。

**切片 C：队列与取下一项（最后做）。**
- **授权池 fail-closed，默认空** —— 一张卡必须被**显式**标记才进池。
  不把所有 `active/` 卡视为可自动实施，不把建议当成批准（任务书原文）。
  **这一条是我定的默认，不是问句** —— 理由：错误方向的自动实施代价远高于多标一次的成本；
  要拦就拦。
- 一批最多 **3** 个任务；同一外部失败最多重试 **2** 次，无进展则保存状态并报告。
- 停止条件：队列结束 · 用户叫停 · 权限拒绝 · 配额耗尽 · **未授权花费**（AGENTS.md §1，
  唯一还必须问的那件事）。

## 3. 一个本卡不修、但 L5 绕不过的前提：工作落不了地

实测（2026-09-16）：`main` 最后一次合并是 **2026-09-06**，十天没动；
`change/TASK-109-three-pane` 上堆着 **56 个提交**，`change/l4-loop` 又叠在它上面，
本卡这棵树叠在 `l4-loop` 上。

L5 的闭环定义里「跑完同一闭环」**包含合并**。如果自动取下一项只是把提交继续堆在一条
永不落地的分支上，L5 产出的不是交付，是**更快的堆积** —— 这正是 §1 那两条破口的放大版：
没人看着的时候，堆积不会喊疼。

本卡**不修这条**（它是 `change/TASK-109-three-pane` 那条链自己的收口问题，
换实施者会违反 AGENTS.md §14），但把它记为切片 C 的**前置条件**：
**C 开工前，这条链必须已经落地，或已明确它为什么可以不落地。**

## 4. IN SCOPE / OUT OF SCOPE

**IN SCOPE**：切片 A / B / C 及其 `tests/tooling/` 测试 · ADR-0104 · 删除
`.claude/tmp/l4-claude-handoff-20260915.md`（结论已提炼进本卡与 TASK-147）。

**OUT OF SCOPE**：
- **不降低任何闸门的严格度**。本卡改的是「闸门跑不跑得到」，不是「闸门查多少」。
- **不引入无限循环调用模型**。任务书原文：不把「无限循环调用模型」算作升级。
- 不改产品代码、不碰产品里程碑（REQ-009 切片一仍是主线）。
- 不新建任务分配器 / 锁服务 / 中央协调进程（TASK-143 OUT OF SCOPE，不重访）。
- 不替 `change/TASK-109-three-pane` 收口（§3）。

## 5. 完成判据

1. **切片 A**：在**任意**一棵工作树里提交一个 ruff 必报的文件都会被拒绝 ——
   §1 第 2 条那个探针重跑一次，结果必须反过来。有 `tests/tooling/` 测试钉住。
2. **切片 A**：`PreToolUse` 静默不触发时，提交**仍然**被拦住；且能说出闸门到底跑没跑
   （沉默不再有两种含义）。
3. **切片 B**：五项度量有 append-only 台账与写入入口；能答出「连续 5 个真实任务里，
   几个零工程介入」。
4. **切片 C**：授权池默认空且 fail-closed；一批上限 3、重试上限 2、五个停止条件各有测试。
5. 每个切片收口时如实写「实现完成」与「还没在真实项目上被人看过的」两个独立事实
   （AGENTS.md §1），不拿测试全绿冒充达标。

## 6. 切片 A 交付记录（2026-09-16）

两个独立事实，分开写（AGENTS.md §1）：

**一、实现完成。** 代码级证据：

| 项 | 证据 |
| --- | --- |
| 权威闸门 | `.claude/hooks/pre_commit.py` —— 定根问 git（**被提交的那棵树**，不是脚本住的那棵）· `-z` 取暂存路径 · 复用 `commit_gate_policy.classify()` · 便宜的检查排在前面 |
| 安装器 | `.claude/tools/install_git_hooks.py` —— 写一层不含仓库路径的 sh shim；`core.hooksPath` 被设时**拒绝安装**（否则会「装好了、报告成功、从不触发」）；`--check` 报告漂移 |
| 决策 | [ADR-0104](../../adr/ADR-0104-git-native-hook-is-the-authoritative-gate.md)，ADR-0050 已补上反向取代链接 |
| 测试 | `tests/tooling/test_git_native_gate.py` **16 passed**，含端到端一条：由 **git 自己**调起 shim，提交被拒且 `HEAD` 没动 |

两条判据的对照实测：

- **判据 1**：同一个探针（ruff 必报 I001 + F401 + 格式的文件），2026-09-16 上午在
  `motv-wt/pilot-143` 里**提交成功、闸门零输出**；换成本闸门后，普通仓库与 worktree
  两种情形都被拒（`test_a_ruff_violation_is_blocked_in_a_worktree_too`）。
- **判据 2**：过了也留一行 `[gate] pre-commit ok · 档位 … · N 项检查 · <树>`
  （`test_a_clean_commit_still_says_the_gate_ran`）。沉默从此只剩一种解释：没跑。

**二、还没在真实项目上被人看过的**：

1. **闸门尚未铺开，这是刻意的。** shim 找不到 `.claude/hooks/pre_commit.py` 时
   fail-closed，而这份实现只在本分支上 —— 开发中实测装上之后，同仓另外两个会话的树
   上没有这个文件，**他们的提交会被一起拦住**。当场撤销。安装因此是**合并时的动作**
   （ADR-0104 决策 5），不是现在。**在合并前，第 1 节那两条破口仍然成立。**
2. **`install_git_hooks.py --check` 还没进体检**（`motv_doctor.py`）。`.git/hooks`
   不随仓库分发，新克隆必须跑一次安装器 —— 没有这道提醒，「忘了装」会安静地变回
   今天的状态。ADR-0104 代价一节已记，接线留在本卡收口。
3. **只覆盖 `pre-commit`。** `pre-push` / `commit-msg` 不在范围内。
4. **独立审查未做** —— 见下。

## 7. 独立审查（codex，真 codex，独立性未降级）

| 轮 | 结论 | 买轮的那条（新机理） |
| --- | --- | --- |
| 1 | **fail** · 判据 1 `FAIL` · 判据 2 `NOT_EVIDENCED` · `AGENTS.md §6` / `§20` 两条 `FAIL` · 5 条 BLOCKING | 见下 |
| 2 | **fail** · 判据 2 转 `PASS` · 判据 1 仍 `FAIL`（**新机理**）· 1 P1 + 1 P2 | 见下 |
| 3 | 待跑（新 P1 按 ADR-0081 §2a 买它自己的那一轮） | —— |

轮 1 报出来的五条，**全部成立，全部已修**。其中第一条直接打在本卡的立论上：

| # | 机理 | 修法 |
| --- | --- | --- |
| 1 | **ruff 查工作区、git 提交索引** —— 暂存违规版本，再在工作区改干净但不暂存，违规版本就能提交成功。判据 1 的一个更阴的拼法 | `index_snapshot()` 把**整棵索引**物化到临时目录，ruff 在那里跑。**范围仍是全仓**（只查改动文件被 TASK-143 点名为免罪符），变的是查哪一份全仓 |
| 2 | **git 的仓库身份变量泄进子进程** —— `GIT_DIR` / `GIT_INDEX_FILE` 被继承，pytest 在临时仓库里的 git 会指回正在提交的仓库 | `clean_env()` 摘掉八个身份变量，所有子进程统一走它 |
| 3 | **安装器给不会被执行的 hook 发合格证** —— 按文本比会把 CRLF 归一掉；可执行位没查（Ubuntu 上 git 静默跳过） | `hook_state()` 按**字节**比 + 查可执行位；安装后**再验一次**才报成功 |
| 4 | **测试自己在 Ubuntu 上会红** —— 复制过去的解释器没有可执行位，shim 的 `-x` 落空 | 端到端改为链接真 venv（symlink，Windows 回退 junction，都不行则 skip） |
| 5 | **裸 `git` 调用**违反 AGENTS.md §6 | 两个文件都改为 `shutil.which` 解析、失败即 `Blocked` |

判据 2 的 `NOT_EVIDENCED` 也已闭合：原来的端到端用的是「直接 exit 1」的桩，
证明的只是「shim 会调起某个东西」。现在把**真闸门**（`pre_commit.py` +
`commit_gate_policy.py`）连同一个可用的 `.venv` 接进临时仓库，两条都跑真的：
违规提交被拒且理由是 ruff，干净提交落地且输出 `[gate] pre-commit ok`。

**一条顺带的范围决定，不是放松**：去掉了 `git diff --check`（工作区空白字符），
保留 `git diff --cached --check`。前者问的是「你还没暂存的东西里有没有空白问题」——
与这次提交无关，而且正是 TASK-143 第 2 种形状（别人的在制品挡住我的提交）的机理。
记在 ADR-0104 决策 5，不留在这里当孤证。

修复后验证：`pytest tests/tooling -n 8` → **481 passed / 2 skipped**（新增 23 条）·
`ruff check` + `format --check` 干净（742 文件）· `lifecycle_check` 0 finding。

### 轮 2：一条**新 P1**，而且是轮 1 那条修复自己引入的

判据 2 已转 `PASS`（真闸门端到端两条都认了）。判据 1 仍 `FAIL`，但**换了机理**，
按 ADR-0081 §2c（看失效机理、不看代码位置）判为新 P1，买它自己的那一轮：

| # | 机理 | 修法 |
| --- | --- | --- |
| 1（P1） | 轮 1 为了不污染子进程，把 `GIT_INDEX_FILE` 一起摘了。但 `git commit -a` 与 `git commit -- <路径>` **不写普通索引** —— git 另建一个临时索引并用这个变量指过去。摘掉它，闸门查的是普通索引：**普通索引干净就放行，而真正被提交的那份含违规内容** | 拆成两个环境：`gate_env()` 给闸门自己的 git（原样继承，包括临时索引身份），`clean_env()` 只给被拉起来的检查工具 |
| 2（P2） | 安装器直接覆盖已有的 `pre-commit`，**静默删掉**那个仓库原有的检查 —— 一个以「闸门不许安静消失」立论的工具，不能自己去让别的闸门安静消失 | 认签名：不是本工具装的就拒绝并提示 `--force`。拒绝的是**静默**覆盖，不是覆盖本身 |

这是同一个病的**第三种拼法**：查工作区（轮 1）→ 查旁边那棵树（本卡起点）→
查另一份索引（轮 2）。三次都是「被检查的东西 ≠ 被提交的东西」。

`VERIFICATION: INSUFFICIENT` 一并闭合：新增 `commit -a` 与 `commit -- <路径>` 两条
真提交测试。**这两条守得住是验过的** —— 把修复临时退回去，
`test_commit_dash_a_is_checked_against_gits_temporary_index` 当场变红。

轮 2 修复后验证：`pytest tests/tooling -n 8` → **485 passed / 2 skipped** ·
`ruff check` + `format --check` 干净（742 文件）· `lifecycle_check` 0 finding。
