# TASK-074：第四阶段 —— 后期交付、旧数据迁移、旧页面与旧接口清理、真实项目验收

- 状态：**已规划，未开始**（前置 TASK-073 未完成）
- 实施基线：**`ae0a54a`** + TASK-072 / TASK-073 的交付
- 负责 Agent：单一实施 Agent（AGENTS.md 第 14 条）
- 依据：[ADR-0066](../adr/ADR-0066-product-refactor-fixed-ia-review-layers-and-system-contract.md)、
  [创作者系统合同](../design/creator-system-contract.md) §10
- 前置：[TASK-073](TASK-073-fixed-ia-and-contextual-agent.md) 全部验收通过
- 性质：**这是唯一允许删除的阶段。** 前三阶段一律并存。

## 0. 本轮边界

四件事，顺序不可交换：

```
1 后期交付补全  →  2 旧数据迁移  →  3 真实项目全流程验收  →  4 旧页面与旧接口清理
```

**清理排在验收之后**，因为「删掉之后才发现真实项目还在用它」是不可逆的。

## 1. 交付

### 1.1 后期交付页补全（IA §4 ⑩）

Delivery 的生命周期（候选 → 质检 → 用户确认导出 → Final）已由**系统合同 §6.5**
冻结，本卡按它实施；§1.2 是其中的「质检」一步。


TASK-073 已把七个分区搭起来，本轮补齐真实能力：

| 分区 | 补齐内容 |
| --- | --- |
| 时间线与粗剪调整 | 已有（`timeline.js` / `postconsole.js`），只做接线验证 |
| 配音 | 对白轨按镜头对齐；本地 TTS；Base Voice 关联 |
| 环境音 / 音效 / 音乐 | 已有 `shotaudio.js`，补 Episode 级 BGM 轨 |
| 字幕 | Case A（有对白 + 时序）已有；Case B/C **显示 not available，绝不伪造听写** |
| 成片预览 | 完整播放 + 与规格对照 |
| **交付质检** | **新增**：音画同步 / 字幕 / 音量 / 黑帧 / 缺帧 / 规格 / 素材权限；三级 severity |
| 导出及导出记录 | 新版本 append；导出记录含规格、耗时、成本、确认人 |

明确不做：复杂节点、专业调色、多机位、插件系统、无限轨道。

### 1.2 交付质检（检查层 3）

| 检查项 | 判定方式 | 默认 severity |
| --- | --- | --- |
| 音画同步 | 对白轨锚点 vs 视频时基偏移 | blocking（超阈值） |
| 字幕 | 存在性 / 时序越界 / 空 cue | warning |
| 音量 | 整体响度 + 削波检测（ffmpeg） | warning（响度）/ blocking（削波） |
| 黑帧 | ffmpeg blackdetect | warning |
| 缺帧 | 时长 vs 帧数一致性 | blocking |
| 规格 | 分辨率 / 帧率 / 码率 / 时长 / 容器 vs 项目成片规格 | blocking |
| 素材权限 | 每个用到的 Asset 的来源标记 | blocking（未标注来源） |

- **它不是创意审片**：发现「这场戏不好看」不属于本层，退回检查层 2。
- 检测能力缺失时（未装某工具）→ 该项显示 `unavailable` + 原因，
  **绝不产生一条「通过」的结论**（沿用 ADR-0064 决策 6 的诚实规则）。
- G4：存在 open 的 blocking 问题时 `exportDelivery` **拒绝执行**并列出问题。

### 1.3 旧数据迁移

| 数据 | 迁移 |
| --- | --- |
| canvas v14 及更早 | TASK-072 已提供 v15 迁移；本轮验证真实项目文档 |
| `approveShot` 双写 | 删除旧布尔标记，只留层 1 Decision |
| 无 `locatedShotId` 的历史整集问题 | 若存在：迁移为 `layer=shot` 或标记 `needs_relocation`，**不丢弃** |
| 历史 Skill Run 的 `provider` / `model` 空值 | 保持 `null`（「没记录」是真相），**不回填猜测值** |
| 旧生成记录无 `cost` | 保持缺失并显式标注「未记录」，不填 0 |

**硬规则：不删除任何用户数据。** 迁移是加法或派生；schema 迁移确定性
（不用时钟、不用随机、不猜）。

### 1.4 真实项目全流程验收

**在真实 Connected Project 上跑完整流程一遍**（AGENTS.md 第 20 条：
demo seed 与 SVG 占位素材不作为主要验收依据）：

```
创建项目 → 设置规格/预算 → 创意 → 大纲 → 设定 → 分集 → 剧本(锁定)
→ 分镜 → 绑定参考 → 主画面 → 视频候选 → 单镜定稿
→ 自动粗剪 → 整集审片(含一次退回重做) → 后期 → 质检 → 导出
```

必须验到的边界情况：

1. 镜头未全定稿时提交正式审片 → **被 G1 拒绝**，可生成测试粗剪。
2. 审片通过后改镜头顺序 → **审片状态回落 `needs_rereview`，画面锁定解除**（G3）。
3. 存在 blocking 质检问题时导出 → **被 G4 拒绝**并列出问题。
4. 长任务运行中刷新页面 → **状态从后端恢复**。
5. 长任务取消 → **子进程真实退出**。
6. 后端不可用 → 页面显示错误，**不显示空列表**。
7. runtime 不可用 → 手工兜底路径可完成同一动作。
8. 每次粗剪 / 每次导出 → **新版本，旧版本仍在**。

**发现真实数据问题时优先如实报告，不得用 mock 绕过。**

### 1.5 旧页面与旧接口清理

**只有 1.4 全绿之后才执行。**

| 清理对象 | 条件 |
| --- | --- |
| `/api/skill/run` 同步分支 | 前端已全部走 `run_id` 路径 |
| `/api/agent/*` 五个创作端点 | ADR-0065 / TASK-068 收口完成 |
| `services/query.js` 的写函数 re-export | 无调用点 |
| `run.skillRunId` 兼容别名 | 全部读写已改用 `runId`（系统合同 §5.0） |
| `approveShot` 旧布尔标记 | 迁移完成 |
| 只服务旧入口的 UI 模块 | **逐个确认无引用后**再删；有疑问就保留 |
| `ui/shotgraphview.js` / `workflow/shotgraph.js` | **保留**——它们是「生成记录」与诊断视图的实现（ADR-0066 §4） |
| `ui/wfgraph.js` / `workflow/provenance.js` | **保留**——ADR-0052 决策 1/2/3/5/6/7 仍然 Accepted；只删「工作流」顶层入口与画布的执行路径，读模型一行不动 |

清理的三条规则：

1. **先确认无引用，再删。** `grep` 全仓 + 前端测试全绿。
2. **能力已在别处覆盖才删入口；能力本身不删。**
3. **有疑问就保留。** 一个多余的文件比一个丢失的能力便宜得多。

### 1.6 文档收口

- ADR-0052 / 0064 / 0065 / 0066 已于 2026-08-13 全部转 Accepted，
  ADR-0057 / 0061 / 0063 的替代关系注记已加好（**未修改原决策文字**，
  AGENTS.md 第 18 条）。**治理状态无残留项**，本轮只复查一遍注记是否与最终实现一致。
- 更新 [端到端需求追踪矩阵](../design/end-to-end-requirements-traceability.md)。

## 2. 依赖

```
TASK-073 全部验收
   ↓
1.1 后期交付补全 ──→ 1.2 交付质检
   ↓
1.3 旧数据迁移
   ↓
1.4 真实项目全流程验收        ← 硬门槛
   ↓
1.5 旧页面 / 旧接口清理  ──→ 1.6 文档收口
```

## 3. 验收标准

| # | 标准 | 验证 |
| --- | --- | --- |
| 1 | 真实 Connected Project 完整跑通一集 | 手动全流程 + 产出成片文件 |
| 2 | 八个边界情况全部符合预期 | §1.4 逐条 |
| 3 | 质检七项各自可产出真实结论或诚实的 `unavailable` | 定向测试 |
| 4 | 阻断级问题真的阻断导出 | 注入 blocking 的守卫测试 |
| 5 | 历史数据迁移后无丢失 | 迁移前后对象计数与抽样比对 |
| 6 | 清理后全量测试绿 | 全量 pytest + 全量前端 + ruff |
| 7 | 清理未删除任何仍被引用的能力 | 全仓 grep + 测试 |
| 8 | 文档之间无冲突权威描述 | ADR 注记 + 追踪矩阵复查 |

**风险等级：高**（持久化 + schema 迁移 + 渲染/文件操作 + 存储生命周期 + 删除）
→ **全量 pytest + 全量前端 + ruff + Codex 独立审查**，且清理阶段单独再审一次。
