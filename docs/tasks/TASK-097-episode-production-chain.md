# TASK-097：剧集制作这条链 —— 一个对话框从头做完

- 状态：**未开工**
- 负责 Agent：**单一实施 Agent，一个对话框做完全部五段**（AGENTS.md 第 14 条）
- 依据：产品负责人 2026-08-17 —— **「那你的工作就是把这些需求全部落实到 task 层面。
  中间所有问题自己决定。我暂时提出了我所有需求了已经。之后只能是做出来看到成果
  我才能知道哪里不对了。」**
- **前置：[TASK-094](TASK-094-story-development-chain.md) 整条链完成**
  —— 向导第 ① 步要吃分集规划的内容，那个形状是 094 定的
- 详细规格：[TASK-092](TASK-092-shot-workflow-multi-stage.md) ·
  [TASK-083](TASK-083-phase3-adrs-first.md) ADR-A ·
  [TASK-093](TASK-093-one-canvas-per-shot.md) ·
  [TASK-095](TASK-095-episode-production-wizard.md) ·
  [TASK-096](TASK-096-post-video-three-steps.md)
  —— **五张卡不合并、不废弃**：它们逐字保存了产品负责人的原话与实测证据。
  本卡只负责**顺序、共同机制、链的收口**。
- 验收环境：**真实 Connected Project `照见未明rev2`**

---

## 0. 授权范围

产品负责人 2026-08-17 明确：**需求已提完，中间所有问题由实施方自己决定，
他只在看到成果后判断哪里不对。**

因此本链**不设任何「先问产品负责人」的闸门**，唯一例外是 CLAUDE.md 那条
**不可让渡**的：**真实花钱**。本链实现的是付费的**闸门与流程**；
每一次实际扣费仍由他在界面上按 ADR-0041 的两步确认。

遇到「回不了头」的实现方式，**默认动作是把它变成可逆的**（带版本的新路径、
加法字段、软删除），不是停下来问（AGENTS.md 第 13 条 / CLAUDE.md 决策模式）。

## 1. 五段与不可调的顺序

```
TASK-094 整条链（故事开发 —— 分集规划定型）
    ↓
① TASK-092   Shot 六个 stage + ④→⑤ 那道闸门          状态层
    ↓
② ADR-0071   有序参考集合实施（TASK-083 ADR-A）        参考层
    ↓
③ TASK-093   单镜画布可编辑                            执行面
    ↓
④ TASK-095   五步向导（A→B→C→D→F→G→E）                主线
    ↓
⑤ TASK-096   配音·音效 → 剪辑 → QC                     收尾
```

**每一步依赖前一步的理由（都是硬的，不是保守）**：

| 若顺序反了 | 会发生什么 |
| --- | --- |
| 095 早于 092 | ④→⑤ 那道门被做成一根箭头，`skipped` 无处可存 |
| 095 的 D 早于 ADR-0071 | 合出的 `@角色` 在付费路线上**一张图也送不到模型** —— TASK-077 §1.3 修掉的那个谎原样复活 |
| 095 的 G 早于 093 | ⑤ Keyframe **没有执行面**，只能退化成一个按钮，而按钮表达不了「哪几张图、什么顺序、每张管什么」 |
| 096 早于 095 | 音频没有画面可对齐，QC 没有素材可判 |

## 2. 共同机制：做一次，五段复用

**批次 0 先抽出来。** 与 TASK-094 §1 同样的道理：不抽，同一套东西会被摸索五遍。

### 2.1 「一次生成 = 一张卡」

Prompt + 参考 chip + 模型 + 规格 + **报价** + 提交，**同一张卡**。
本链有五处生成（资产图 / 提示词合成 / Storyboard / Keyframe / Video），
**全部用同一个组件**。TASK-078 批次 B 已经做了「可见」那一半，本链补齐。

**报价只来自 Gateway preflight，界面永不自算**（ADR-0071 决策 6）。

### 2.2 有序参考集合 + **用法规则**

`[[ref:N]]` 内联引用 + 每张图**声明它管什么**（构图 / 身份 / 环境 / 风格）
+ 一段**用法规则**（TASK-095 §2.3.3）。

**规则是 Skill 包的内容，不是硬编码字符串** —— 它会随经验演进。
不带规则的后果是确定的：四视图设定图会让模型画出四个视图。

### 2.3 批量付费的统一形状

本链有三处批量（一键合成全部提示词 / Storyboard 全集 / 批量生视频）。
**一套代码**：

1. preflight 给**总额**，不是单价 ×N 让人自己乘
2. 一次确认（ADR-0041 两步）
3. **可中止**
4. 中途失败**不把整批标成成功**，已花的钱**如实记账**
5. provider 未声明「多图不额外计费」→ `maxImages: 0` → **fail-closed 拒绝并说明原因**
   （ADR-0071 方案 C），**不静默降级成单图**

### 2.4 「状态只有一份」

092 的六个 status 是**唯一真相**。本链所有界面（向导进度 / 画布节点 / 故事板审片 /
⑦ 分镜表 / QC 报告）都是它的**读者**。
**守卫测试：不得出现第二份计算。** TASK-079 已经立过这条，本链不许破。

### 2.5 计数必须来自实际登记表

「还差 10 个」「1/14 已合成」「14 个镜头已就绪」—— 每一个数字**查登记表**，
不查清单上的空。

前车之鉴是本仓库自己的：`storageState` 声明「文件在」而从不核对，
存储页因此报「媒体不可用 0」。**本链每一个计数都是同一个陷阱的复现机会。**

### 2.5b 批次 0 已经把两条不变量打硬了 —— **后续批次消费它，不得重新推导**

> **2026-08-18 补记，依据是批次 0 的 15 个 P1**（全真、零驳回）。
> 本节优先级与 §2.6 同级，因为它直接决定剩下十一个批次的轮次成本。

批次 0 的 15 个 P1 **不是随机分布的**，它们只落在两条不变量上：

| 不变量 | 那批 P1 在说什么 |
| --- | --- |
| **文本 ↔ 集合必须一起改** | 最危险的一条：剔除一张参考图后重新编号，`[[ref:N]]` **静默改指到另一张图**。还有：重复条目 id、无 id 条目被静默跳过、隐式 ordinal 不参与冲突检测、`validateReferenceSet` 不校验版本与 digest |
| **账 ↔ 现实必须对得上** | 总额没币种 / 不说覆盖几条 / 与本批条数不等；免费条目 `spent: null` 永远算「未知」；中止后迟到的回执被丢弃；被拒绝的批次显示成「进行中」；结算总额为 0 时账单行被省略 |

**这两条本卡此前写成了「价值」而不是「不变量」**（§2.3 第 4 条原文是
「中途失败不把整批标成成功，已花的钱**如实记账**」）。
「如实」不可测试；「`total` 必须带币种、必须声明覆盖几条、且必须等于本批条数」可测试。
**那九个金额相关的 P1，全是那一句概括没能枚举、也不可能手工枚举出来的分支。**

这正是 §2.6.1 那条判据打在本卡自己身上：**卡里写了一句概括性的要求，
那就是一处待办的不变量，不是规格。**

#### 现在它们已经是代码了。后续批次的硬约束：

| 模块 | 导出 | 谁必须用 |
| --- | --- | --- |
| `src/workflow/refset.js`（472 行） | `normalizeReferenceInputs` · `normalizeReferenceSet` · `reorderReferenceInputs` · `refMarkers` · `validateReferenceSet` · `compileReferenceMarkers` · `remapMarkers` · `usageRuleFor` · `usageRuleBlock` · `categoryOf` · `modelReach` · `groupByCategory` | **批次 2 · 4C · 4D · 4G** |
| `src/workflow/batchpay.js`（396 行） | `createBatch` · `applyPreflight` · `confirmBatch` · `abortBatch` · `recordItem` · `settlement` · `settlementLine` | **批次 4D · 4F · 4G · 4E** |
| `src/workflow/genspec.js` | `isDisplayableAmount` · `genSite` · `specViolations` · `quoteView` · `specRows` | **五处生成全部** |
| `src/workflow/counts.js` | `productionCounts` · `countText` · `isDeleted` · `UNKNOWN` | **每一个计数**（§2.5 / §2.6.2） |
| `src/workflow/refscan.js` | `foreignReferences` · `isReferenced` · `findPaths` | **软删除 shot / 归档 / QC 缺口**（§2.6.1） |

**「重新推导」的判定很简单**：如果一个批次里出现了金额校验、ordinal 冲突检测、
参考文本改写、批量状态机或计数口径的**新代码**，那就是重新推导 ——
**改成调用上面的导出，或者扩展那个模块本身**（扩展要带守卫）。

**理由不是复用洁癖，是那 15 个 P1 的代价已经付过一次。** 重新推导等于重新踩，
而且新踩的那份**没有 6 轮 codex 看过**。

#### 可证伪的预测（写在这里，链尾对照）

批次 0 之所以最贵，是因为**共享机制必须一次性吃下所有调用方的边界情况**——
它是前置保险，不是常态。批次 1（枚举清晰的状态模型）3 个 P1、轮 3 干净通过，
已经是一个数据点。

**因此预测：看起来最吓人的 4D（提示词编译器）与 4G（付费路径多图合成），
恰恰是受益最大的两个** —— 它们要的正是上面两条已被打硬的不变量。
**若它们消费而不重新推导，其轮次应低于体量给人的直觉。**

**链尾请对照这个预测记录实际轮次。** 若 4D / 4G 仍然高轮次，
说明本节的读法是错的，那本身就是下一条链最值钱的输入。

### 2.5c 接线账（2026-08-18，依据批次 2 的第 6 轮发现）—— **链尾不得有零调用方的导出**

实施方在批次 2 报告：

> 「守卫全绿不等于交付了。第 6 轮指出我新写的两个函数**只有测试在调用** ——
> 能力读取、违规判定、变异验证全都做了，而运行中的应用照旧无条件说
> 『参考图不会进模型』。**在未接线的模块上测试通过，读起来与功能完成一模一样。**」

**这是本仓库那个缺陷家族的新变体。** 此前是「守卫看起来加了，其实只覆盖了一半」，
这一条是「**代码看起来做完了，其实没有任何调用方**」——
而且它比前者更难发现，因为**测试是绿的、审查是过的、diff 是完整的**。

#### 今天的事实（派生扫描，不是手写清单）

```sh
cd mockups/motv-workspace
for m in refscan refset batchpay counts genspec; do
  n=$(grep -rl "from ['\"].*$m\.js['\"]" src/ | grep -vc test)
  echo "$m  应用侧调用方 $n"
done
```

2026-08-18 的结果：

| 模块 | 行数 | 应用侧调用方 | 状态 |
| --- | --- | --- | --- |
| `refscan` | 96 | 1（`episodecleanup.js`） | ✅ 已接线 |
| `refset` | 472 | 1（`gencard.js`，批次 2 接上） | ✅ 已接线 |
| `genspec` | 189 | 2（`gencard.js` / `batchpay.js`） | ✅ 已接线 |
| **`batchpay`** | **396** | **0** | ⚠️ **死代码**，排 4D/4F/4G/4E |
| **`counts`** | **171** | **0** | ⚠️ **死代码**，排 4A/4B/4C/4F/4G |

**567 行经过 6 轮 codex 审查、今天在运行的应用里没有任何调用方。**
而 `batchpay` 正是批次 0 那 15 个 P1 里**九个**的落点 ——
**代价最高的那部分代码，目前一行也没跑起来。**

#### 硬规则

1. **每个批次收口前跑上面那条扫描**，把结果填进 §4 进度表的备注。
   **数字只能减不能增**：批次结束时零调用方模块的数量必须 ≤ 开工时。
2. **链尾零调用方必须为 0。** 若某个导出到链尾仍无人调用，
   只有两条出路：**接上它**，或**删掉它并说明为什么当初判断错了**。
   **不允许「留着以后用」** —— 那正是「读起来与功能完成一模一样」的成因。
3. **新导出同批必须有真实调用方。** 批次里新增的 export，
   收口时必须能指出**一条从界面到它的非测试调用路径**。
   指不出来的，要么本批接上，要么不要在本批引入。
4. **风险最高的是 `counts`**：§2.6.2 已经写明「每个计数都有多个消费者」。
   若某批就地算了一遍计数而没走 `counts.productionCounts`，
   结果是**模块永远接不上 + 多出第二份计数** —— 两个缺陷一次性达成。

### 2.5d 「枚举 → 派生」在第二个领域被证实了一次（批次 2 的 SSRF）

实施方：**「修了六轮才从『枚举』换成『白名单』。」**
前五轮每轮以为修完了类，实际只修了当轮被指出的那个拼法
（scheme → 十/十六/八进制 → 点分十六进制 → 尾部根点 + 全段十六进制 → CGNAT）；
第六轮换成 `is_global` + 「必须长得像 DNS 名」后一次覆盖整族。

**这与 §2.6.1 是同一条**（手写的「N 项」清单会漏），
现在它在**第二个完全不同的领域**（安全）被独立证实了一次。**两个领域，同一形状。**

#### 但它带来一条 §2.6.1 没有的补充：**过严的守卫等于没有守卫**

实施方差点把 `example.ca` / `example.de` 拒掉（ccTLD 多是纯 a–f 字母，
撞上「全段十六进制」那条规则）。他的话值得逐字留下：

> **「挡住正常域名的守卫会被绕开，然后什么都保护不了。」**

因此他的测试**同时钉住两个方向**：既要拒掉绕过拼法，也要放行正常域名。

**本链的适用处** —— 每一条 fail-closed 都要钉两个方向：

| 闸门 | 也必须证明它**放行**什么 |
| --- | --- |
| ADR-0071 方案 C（provider 未声明多图不额外计费 → 拒绝） | 已声明的 provider **必须真的能用** |
| ④→⑤ Storyboard 闸门 | `skipped` 与已 approved **必须真的放行** |
| 软删除 shot 的引用检查 | 无人引用的 shot **必须真的能删** |
| QC 的时长超差 | 在容差内的 **必须不报警** |

**只钉「会拒绝」的一半，就是在造一个迟早被关掉的闸门。**

### 2.5e 缝，才是缺陷住的地方（批次 3，9 个 P1 全在这里）

实施方报告：批次 3 六轮 9 个 P1，**没有一条是领域模块的逻辑错**。
纯判定模块 `canvasnodes` 六轮**零 P1**。全部 9 条落在
**「判定」与「消费判定的人」之间的缝上**：

- `available` 却**没有处理器**
- 丢掉 prefill **却说会带过去**
- 把 in-flight 归给**没有流水线**的 stage
- 一个**只可能拒绝**的闸门

#### 这就是本项目那个缺陷家族的一般形式

把这条链和审计里的每一个缺陷放在一起看，它们是**同一句话**：

> **系统里有两处在陈述同一件事实，而它们没有共用同一份事实。**

| 实例 | 两处 |
| --- | --- |
| `¥0` 预算 | 显示 vs 真实成本 |
| 「媒体不可用 0」 | `storageState` 的声明 vs 磁盘 |
| 「模型直接输入」 | 界面标签 vs Provider 真实能力 |
| 归档扫描漏 `timelines` | 手写清单 vs 实际引用 |
| 16 / 48 | 规划页 vs 另外四个消费者 |
| 未接线模块（§2.5c） | 测试里的现实 vs 应用里的现实 |
| `removalCheck`（下面轮 4） | 测试用的谓词 vs 生产用的谓词 |
| `INCONCLUSIVE`（下面轮 5） | 文档措辞 vs 探针语义 |

**因此 4A–5B 的审查重点不在新模块里，在新模块的接缝上。** 每引入一个判定，
必须同时回答：**谁消费它？消费者能不能做到它承诺的事？**

#### 轮 4：§2.5d 被字面印证 —— 但它暴露了 §2.5d 不够

`removalCheck` 的**生产谓词**写错（把资产自己的版本记录算成外部引用），
于是什么都永远删不掉；**而测试用的是另一个谓词**，所以
「无人引用的真的能删」那一半**在测试里通过、在产品里恒假**。
实施方的话：**「闸门是 born closed 的。」**

**§2.5d 因此加一条硬约束**：

> **两个方向必须用生产那一份谓词来钉，不得用测试自己写的等价物。**
> 谓词要有名字、可导出、生产与测试共用同一份。
> 否则「钉了两个方向」这句话本身就是一处新的缝（见上表）。

#### 轮 5：根因在文档里，不在代码里 —— **而那句话是本仓库自己写的**

`INCONCLUSIVE`（问过、答不上来）被算成产物存在，闸门会在未经确认的媒体上打开。
根因是 ADR-0073 决策 2 的原措辞「探针**没有判定它 MISSING**」——**实现照它写的**。

**追溯：那句话来自 [TASK-092](TASK-092-shot-workflow-multi-stage.md) §2.2 的表**
（本卡作者写的），经 ADR-0073 进入实现。实施方订正了 ADR，
**TASK-092 的两处已于 2026-08-18 一并订正**（提交 `7f7719b`）。

> **规则：修完代码要问「这个错的说法是从哪抄来的」，然后把源头也改掉。**
> 只改代码，下一个人会照同一句话再写一遍。
>
> **仍待订正**：`src/workflow/shotstage.js:14` 的模块头注释仍以宽松措辞下定义
> （代码已正确）。留给下一个提交顺手带上 —— 读那个文件的人先读到的是那句。

### 2.5f 闸门的三条（批次 4A 买来的，**4B–5B 每一批都有状态显示，会一路复现**）

#### 一、「不知道」不是「放行」

`readyOf` 在上游答不上来时落进 `else`、给不出阻塞，于是第 ③ 步**亮着「进入」，
点进去是一个空页**。

这是 §2.5b 那条「不知道 ≠ 0」在**闸门**上的同一形状：
**不知道 ≠ 放行。**

> **每一个判定都必须为「上游答不上来」留一条显式分支，
> 而那条分支不得是宽松的那一边。**
> 落进 `else` 就是把「我不知道」实现成了「可以」。

实施方顺手做对了两件事，都要在后面的批次保持：
- 上游要求**从手写改成由步骤次序派生**（§2.6.1 —— 这次手写清单漏的正是
  「第 ③ 步有没有检查上游是否可知」）。
- 判定提成**导出的纯函数 `stepReadiness`** —— §2.5d 要求钉住的必须是**生产用的
  那个谓词**；藏在 `app.js` 闭包里，测试只能钉一份等价物，**那本身就是新的一条缝**。
  变异证明 14/15。

#### 二、`blockers` 只表示「开始不了」——**不要把用户要做的工作印成拦住他的理由**

它的镜像，**测试全绿、只有看真实屏幕才抓到**：把「自己还没做完」也算成阻塞，
于是第 ② 步 —— **正是此刻该做的那一步** —— 顶着一句
「还不能开始这一步 · 还有 1 个没有设定图」。

> **这一步自己的待办 ≠ 这一步的阻塞。**
> 前者是「你要做的活」，后者是「你还不能开始」。**混起来，界面就在拦住它请你做的事。**

**4B–5B 的适用面（每一批都有）**：分镜表的「还差几镜」、资产准备的
「还差 10 个设定图」、Storyboard 的「还有几镜没通过」、Keyframe 的闸门提示、
QC 的缺口 —— **每一处都要先分清它是待办还是阻塞。**

#### 三、同一件事被决定两次 —— 兜底默认值会压掉真正的决策

「打开在哪一步」被决定了两次：shell 的 `|| "shots"` **压掉了**模型选的
「第一个没做完的那一步」。

这是 §2.5e 那张表的又一行（两处陈述同一件事实，没共用同一份）。
**特殊之处在于其中一处是「兜底默认值」** —— 它看起来无害，
实际上把上游的判断静默丢掉了。

> **写 `|| 默认值` 之前先问：上游是不是已经决定过了？**
> 如果是，兜底就不是兜底，是覆盖。

**另记一条**：`ctx.wizard` 被文件后面的旧演示向导**静默覆盖**（已改名 `prodWizard`）。
应用长大之后命名空间会撞，而撞了**不报错、只是行为变成另一个**。
本链后面几批还会往同一个 `ctx` 上挂东西 —— **挂之前先 grep 那个名字。**

### 2.6 从 TASK-094 那条链买来的三条教训（2026-08-18，**本节优先级高于 §2.1–2.5**）

094 那条链 10 个批次、codex 每轮独立审查、链尾 3278+6 项 pytest 与 1452 项前端全绿。
**它交出的最有价值的东西不是代码，是下面这三条** —— 本链有 13 个批次，
同样的陷阱有十几处复现机会。

#### 2.6.1 任何「检查这 N 项」的清单都是可疑的 —— **要派生，不要手写**

094 的收口卡给了四项核对清单（剧本 / 分镜 / 资产绑定 / 生成记录），
**漏了 `timelines`**，而 `timelines` 引用着四个剧集。
实施方把核对做成**派生扫描**，第一次跑就抓到了。
**手写清单会归档掉仍被引用的剧集。**

**本链的适用处（每一处都必须派生，不得手写枚举）**：

| 处 | 不得手写的是 |
| --- | --- |
| §2.5 的每个计数 | 「哪些登记表算作有内容」 |
| ⑤ Keyframe 的输入 | 「哪些资产参与合成」 |
| TASK-096 §2.4 的 QC 缺口 | 「哪些 status 算缺」 |
| 新增 `storyboard` / `keyframe` kind | 「哪些地方消费 asset kind」 |
| 软删除 shot | 「哪些地方引用 shotId」 |

**判据**：如果我在卡里写了一个「N 项」的列表，**那就是一处待办的派生**，不是规格。

#### 2.6.2 每个计数都有多个消费者 —— **改一个，其余继续说谎**

094 的实测：分集规划页已经显示 16，而**rail 徽章、「共 48 集」、EPISODES 列表、
Director 四处仍然显示 48**。这与审计里 GAP-06（目标 24 / 规划 12 / 实体 48
三个数字互不校验）是**同一个形状**。

**本链每引入一个数字，必须先枚举它的全部消费者并一起改**：
`14 个镜头已就绪` / `0/10 已生成、还差 10 个` / `1/14 已合成` /
Storyboard 通过数 / Keyframe 通过数 / 视频完成数 —— 每一个都要问
**「还有谁在显示这件事」**，而答案要**派生**（见 2.6.1），不是回忆。

#### 2.6.3 守卫会静默失效，而 fixture 会**发明字段**

094 查出三个**已经什么都不拒绝**的守卫：一个版本钉死的 digest 测试、
一个只认 version 1 的 `skill_digests()` 查表、一个名为「outline cap works」
实际在量 `prompt.md` 长度的断言。**它们全是绿的。**

**更值得记的是实施方自己那个 `finishedAt` bug**：
测试 fixture **发明了一个不存在的字段**，于是守卫保持绿色，而界面是错的。

**本链的纪律**（本链几乎每个批次都靠守卫测试守着不变量，因此这条是硬约束）：

1. **守卫必须派生**，不得钉死版本号、字段名清单或计数常量 ——
   钉死的守卫会在下一次版本变更时静默变成永真。
2. **fixture 必须从真实文档构造**（真实项目的导出或它的裁剪），
   **不得手写一个「看起来像」的对象** —— 手写的 fixture 会发明字段，
   于是守卫在校验一个产品里不存在的世界。
3. **每个新守卫都要有一次「它真的会拒绝」的证明**：
   先让它失败一次，再让它通过。
   （本仓库已有先例：`tests/test_motv_banner_narrow_console.py` 的第一个测试
   就是证明那个隐患真的会复现的控制测试。）

#### 2.6.4 顺带：**去看真实屏幕**，测试不替代它

094 在真实项目上看屏幕，查出四个全部测试都放过的缺陷：`标题` 列渲染成空、
每一行都在训人「0 条 · 少于建议的 3～6 条」、`故事核心` 同一段打印两次、
以及 2.6.2 那个 16/48 不一致。

**本链每个批次收口前必须在 `照见未明rev2` 上真的打开看一眼**，
不是「测试绿了」。这与 AGENTS.md 第 20 条「真实 Connected Project 是主要验收环境」
同一条，但 094 证明了它**不是形式主义**：四个缺陷里没有一个是测试能抓的。

---

## 3. 批次

| 批次 | 内容 | 风险 |
| --- | --- | --- |
| **0** | §2 的共同机制（生成卡 / 参考集合 / 批量壳 / 状态单一源 / 计数守卫） | **高** |
| **1** | **TASK-092** 六个 stage + 闸门（`skipped` 是唯一新增持久化状态） | **高** |
| **2** | **ADR-0071 实施**：有序参考集合 + Provider 声明 + 方案 C 的 fail-closed | **高** |
| **3** | **TASK-093** 单镜画布可编辑（骨架仍派生，新增节点写回既有登记表） | **高** |
| **4A** | **095** 向导骨架 + 剧本搬入（不新增页面的守卫） | 中 |
| **4B** | **095** 第 ① 步：Scene 分组行 + `timeOfDay` + `音效` 列 + 软删除 | **高** |
| **4C** | **095** 第 ② 步：`script-breakdown` 扩输入 + 三 tab + 构图规范进 Skill | **高** |
| **4D** | **095** 第 ③ 步：两份提示词 + `@` 引用 + 用法规则 | **高** |
| **4F** | **095** 第 ④ 步：Storyboard（新 kind + 横向带 + 便宜档 + 通过/重出/跳过） | **高** |
| **4G** | **095** 第 ⑤ 步：Keyframe 合成（闸门 + 画布 + 接首帧那条路） | **高** |
| **4E** | **095** 批量生视频 | **高** |
| **5A** | **TASK-096** 状态显示 + 「可以开始」vs「可以定稿」+ 声音资产落位 | 中 |
| **5B** | **TASK-096** QC 判据 | **高** |
| **链尾** | 全量 pytest（两阶段）+ 全量前端 + ruff + **逐条走 TASK-095 §7 的对账表** | —— |

**这条链比 094 长得多（13 个批次）。** 因此：

- **每个批次结束立即独立提交**，`MOTV_CONTINUOUS_CHAIN=1` 写在提交命令**最前面**
  （PowerShell 写成首行注释；不是环境变量，不得持久化；同一条命令不得带 push/merge）。
- **每完成一个批号，在本卡的进度表里打勾并写下提交号** —— 链太长，
  不落痕迹下一个读者无法接手（本仓库的既有纪律：状态头过期会误导人）。
- **4A 之后：给产品负责人看，但不停下来等**（2026-08-18 订正，见下）。

> ### 订正：4A 是「展示点」，不是「阻塞点」
>
> 本条原写「允许在 4A 之后**停下来**给产品负责人看一次」。
> 产品负责人 2026-08-18 明确：**「继续剩下所有的批次行不行」** ——
> 因此改为**看了就继续**：
>
> - 4A 收口时在 `照见未明rev2` 上抓向导骨架的截图，**贴出来**，
>   一句话说明「这是那条只有一个下一步的路，形态不对现在说」；
> - **然后直接进 4B，不等回复。**
>
> **理由**：4B 之后的批次（分镜表 / Skill 包 / 提示词编译器 / Storyboard / Keyframe）
> **基本不建在向导外壳的形态上** —— 4A 的形态若被推翻，损失只限于外壳本身。
> 让实施方停在那里空等，是拿**确定的浪费**去换一个**低概率的风险**。
> 这仍然符合 CLAUDE.md「做出来给用户看」：**要的是「看」，不是「等」。**
>
> 产品负责人看到不对会直接打断 —— 那是他一贯的工作方式，
> 也是本项目决策模式的原意（「我只能在看到最终结果有问题之后才能给出决策」）。
>
> **「继续所有批次」不等于「加快」。** 付费与持久化批次的审查轮次**一轮都不能少** ——
> 批次 0 那 15 个 P1 全部落在那两处（§2.5b）。宁可链更长，不可让没人看过的
> 付费/持久化修复出门。

## 4. 进度表（实施者边做边填）

| 批次 | 提交号 | 审查 | 备注 |
| --- | --- | --- | --- |
| 0 | `35337e9` | codex 6 轮（名义 3，超额已声明）：15 P1 + 3 P2 全真全修零驳回 | 五个纯模块 `refscan` / `refset` / `batchpay` / `counts` / `genspec`；`gencard` 改读 `genspec`，`episodecleanup` 改用 `refscan`。守卫 52 项，前端 1504 全绿。**第 6 轮 3 处修复未复审**，已登记[待复审清单](../design/pending-codex-rereview.md)——这两个模块今日零调用方，批次 2/4D/4F/4G/4E 会在真实调用语境复审 |
| 1 | `c94fd19` | codex 3 轮：3 P1 + 1 P2，**轮 3 pass 零发现** | [ADR-0073](../adr/ADR-0073-shot-multi-stage-workflow.md) 自行 Accept（技术、不涉付费、不动用户数据）。`shotstage.js` 六个 stage；只有 `skipped` 持久；依赖是数据（加假 stage 的测试当场证明）；`shotStage` 降级为汇总，线性链删除；新增 `storyboard`/`keyframe` kind + 资产库筛选的派生守卫；schema v16。轮 1 第 3 条 P1 **部分驳回**（不重定义审片那个词），改为拆出 `shotComplete()`。前端 1530 绿 |
| 2 | `41d627b` | codex **7 轮**（名义 3，超额已声明）：13 P1 + 8 P2，12+8 为真已修、1 条驳回 | 目录声明能力（缺省 max 0）+ `ProviderRequest.reference_images` 加法 + 方案 C fail-closed（**拒绝不截断**，接在预约锁之前）+ WAL 往返 + preflight 交出能力 + 接进 `gencard`。**第 6 轮最有价值**：指出两个新函数只有测试在调用——守卫全绿而应用照旧说谎。SSRF 修了六轮才从「枚举拼法」换成 `is_global` + DNS 名白名单。**驳回 DNS 解析**（3/5/7 轮同一条，理由已留档）。真实项目 v15→v16 迁移干净 |
| 3 | `550504f` | codex **6 轮**（名义 3，超额已声明）：9 P1 + 1 P2 全真全修，**轮 6 pass 零发现** | `canvasnodes` / `canvasgrow`；第一条纪律做成可执行（`available` 只由 registry 决定，文本便签因此不可用）；六个 stage 上画布（读 092 那一份）；`available ⊆ CHAIN_HANDLED` 结构守卫。[ADR-0074](../adr/ADR-0074-character-from-image.md) / [ADR-0075](../adr/ADR-0075-camera-motion-presets.md) 自行 Accept。**轮 4**：生产谓词把资产自己的记录算成外部引用 → 什么都删不掉，而测试用了另一个谓词（§2.5d 原话）。**轮 5**：`INCONCLUSIVE` 被算成产物存在 → 根因是 ADR-0073 决策 2 措辞太松，**已订正 ADR 正文**。**看真实屏幕**另抓 3 个测试全绿的缺陷 + CSS 守卫抓到 `--warn` 未定义。**接线账：零调用方 2 → 2**（`batchpay`/`counts` 排 4A 之后）。pytest 3309+6，前端 1585 |
| 4A | `468d802` | codex 3 轮（中风险 1 轮 + P1 各买 1 轮）：2 P1 全真全修，**轮 2 / 轮 3 均 pass 零发现** | 向导骨架 `prodwizard.js`（五步、顶部常驻真实数字、不存进度）+ 本集剧本搬进剧集制作（`spaceOf`/`crumbScope` 改集级，**成员集合不变**）。冻结守卫与**向导可见性同一条断言**（`PAGES.length === 11`）。**轮 1 P1**：上游「不知道」时没有阻塞 → 第 ③ 步亮着「进入」而点进去是空 shotwork —— 「不知道 ≠ 0」在闸门上的同一形状（不知道 ≠ 放行）；顺手把上游要求从**手写**改成**由步骤次序派生**（§2.6.1），并把判定提成导出的纯函数 `stepReadiness`，两个方向都用**生产谓词**钉住（§2.5d），变异证明 14/15。**看真实屏幕又抓到那个 P1 的镜像**：我把「自己没做完」也算成阻塞，于是第 ② 步（正是此刻该做的那一步）顶着「还不能开始这一步」——`blockers` 只该表示「开始不了」，已分开并单独走轮 3。另抓 `ctx.wizard` 名字被旧演示向导静默覆盖、打开步骤被 shell 二次兜底覆盖，两者测试全绿。**接线账：零调用方 2 → 1**（`counts` 0→2，只剩 `batchpay`）。pytest 3309+6，前端 1601，ruff 干净 |
| 4B | `6d03fdd` | codex 2 轮（高风险预算用尽）：1 P1 已修，1 条**驳回**（附实测与钉死测试） | Scene 分组行（`sceneplan.js`，`S01` 派生不存、`timeOfDay` 缺就空不猜、「未分配到场景」是**一组 + 一个动作**）+ `timeOfDay` 加法字段（proddoc 写路径 + schema fail-closed：缺席合法、出现必须非空串）+ 镜头**软删除**（`shotdelete.js`）。**软删除的关键决定**：不在 28 个 `draftShots` 消费者里各加一次过滤（那正是 §2.6.1 的手写清单），而是给项目镜像装一个 `installShotMirror` —— 读到的永远是存活的，回收的单独取，**将来新增的消费者天生就对**。随之而来的真实危险在同一处消除：`mergeKeepingRecycled` 让一次「读→改→写回」**不可能**丢掉回收区。`normalizeShots` 补带 `deleted` 标记（它是白名单，历史先例就是「编辑任何字段都会静默擦掉景别」）。**轮 1 P1**：`draftShots = undefined` 会清空回收区，而同一文件的注释写着「只有 null 才真的清空」—— 又一次两处陈述同一件事实；改为 `null` 清空（「没有草稿」必须还能表达，否则会被读成「已知 0 个镜头」）、`undefined` 拒绝。**轮 2 驳回**：报「标记删除会让表格渲染崩掉」，但 `shotTableModel` 从不把 `deleted` 传进 `applyTableEdits`，且真实项目上点删除→保存→撤销全程 0 page error；仍为这条担心补了一个钉死测试。真实项目实测：60 → 59 + 回收区（1）→ 撤销 → 60，场景组头/时间/新建场景全通，验证用的场景与删除**已在同一次运行里撤销**（现仍 0 场景 / 60 镜 / 0 标记；仅多出 2 个不可变草稿版本）。接线账：`sceneplan` 2 / `shotdelete` 1，零调用方仍为 1（`batchpay`）。pytest 3309+6，前端 1617 | |
| 4C | | | |
| 4D | | | |
| 4F | | | |
| 4G | | | |
| 4E | | | |
| 5A | | | |
| 5B | | | |
| 链尾 | | | 全量数字写在这里 |

> ### 交接检查点（2026-08-19，**不是链尾**）
>
> 产品负责人换机器继续作业，按 [ADR-0068 决策 6](../adr/ADR-0068-continuous-modification-chain.md)
> 「交接」与 push / merge 并列 —— 不能把没验证过的状态交出去。链此时停在 **4B 之后**，
> 所以这是检查点，不是链尾（链尾还要逐条走 TASK-095 §7 的对账表）。
>
> | 检查 | 结果 |
> | --- | --- |
> | `pytest -n 8 -m "not serial"` | **3309 passed / 57 skipped**（109s） |
> | `pytest -m serial` | **6 passed**（44s） |
> | 全量前端 | **1619 passed / 0 failed** |
> | `ruff check` / `format --check` | 干净 / 553 files already formatted |
>
> 另跑了一轮 `HEAD~2..HEAD`（4A + 4B 最终形态）的 codex 跨模型补审：**pass，
> 0 blocking**；两条 non-blocking 当场修掉，因为各踩一条本链硬规则 ——
> ① `deleted.by` 允许任意类型（说自己检查过却放行形状不对的东西，比没有守卫更坏）；
> ② `deletionImpact` 只有测试在调用（§2.5c 那条「登记了一个永不发生的能力」），
> 已接进删除动作，**告知不是闸门**。两处均做变异验证（17 项里 2 项转红）。
>
> 交接说明见 [TASK-097 交接说明](../design/task097-handover-2026-08-19.md)。
> **待复审清单的「仍然待办」为空**（只剩 POSIX parent-death 一条纯记录）——
> 那张历史表的状态列停在 2026-08-14，本次已逐行订正。

## 5. 收口

- 逐条走 **TASK-095 §7 的需求对账表** —— 那张表是按产品负责人的原话列的，
  是本链唯一的验收依据。**不是「测试绿了」。**
- 重跑 `src/ui-gap-audit/tools/capture_current.py`，归档旧图，更新
  [manifest](../../src/ui-gap-audit/manifests/screenshots.md)。
- 五张卡的状态头改成已完成并写实施记录。
- Follow-up 一律登记 [TASK-087 总账](TASK-087-followup-ledger.md)，不散落。
- **[待复审清单](../design/pending-codex-rereview.md)** 清空后才可 push
  （ADR-0068 决策 6：可以推迟，不可以跳过）。
