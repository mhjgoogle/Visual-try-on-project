# .env.local —— 换 API key 时你要改的就是这个文件
#
# 用法：把这份复制成 `.env.local`（同一个目录，仓库根），填上你的 key。
#   PowerShell:  Copy-Item .env.local.example .env.local
#
# 改完**保存即生效**：后端每次请求都重读这个文件，不用重启、不用去界面里找输入框。
# 它已经被 .gitignore 挡住（第 20–22 行的 .env / .env.*），永远不会进 Git。
#
# 优先级：这个文件 > 设置页里粘的 > 进程环境变量。
# 你改哪儿哪儿说了算，所以这里排第一。

# --- 用哪一家出图 -------------------------------------------------------------

# pollinations（默认）：不要 key、不要账号、不产生任何账单。2026-09-05 实测跑通。
#                       代价：延迟 2–45 秒，只有一个模型，无 SLA，
#                       且 prompt 会发给一个你与之没有任何协议的第三方。
# gemini：              要下面那把 key。**免费档对出图的配额是 0**（同日实测，
#                       三个出图模型全部 429 `limit: 0`），所以它实际只在你
#                       开了结算时可用 —— 那是按次计费，要走付费闸。
IMAGE_PROVIDER=pollinations

# --- Gemini（只有 IMAGE_PROVIDER=gemini 时才用得上）---------------------------

# 从 https://aistudio.google.com/apikey 拿，用你登 Gemini 网页版的那个 Google 账号。
GEMINI_API_KEY=

# 这把 key 是哪一档？只有两个值：free / paid
#
#   free  = 免费额度（AI Studio 默认给的那种，不要信用卡）。走「账号额度」这条路，
#           不产生按次账单，因此**不需要每次确认金额**。
#   paid  = 你给这个 Google Cloud 项目开了结算，按次计费。
#
# **必须填，而且填错的代价是你的钱**：一把开了结算的 key 与免费额度那把在接口层面
# 长得一模一样，程序看不出来，探测一下就等于拿你的钱做实验。所以由你声明。
#
# 填 paid、或者留空不填 —— 出图这条路都会直接拒绝（403），一个字节都不会发出去；
# 要花钱就走付费那条，它会先把价格报给你再问你。这是 ADR-0100 决策 1 的
# 「拿不准就按计费处理」。
GEMINI_API_KEY_TIER=free
